---
phase: 40.1-homepage-featured-products
plan: 05
type: execute
status: complete
wave: 4
requirements: [CURR-03]
completed: "2026-06-25"
---

# 40.1-05 SUMMARY — Capstone Human Verification

## Outcome

**Approved.** The user manually verified the full featured-products flow against a
local backend across all 7 steps on both `/en` and `/he`. All steps passed.

## Verification Result (per step)

| Step | Area | Result |
|------|------|--------|
| 1 | Admin toggle (checkbox reveals order field, save) | ✅ Pass¹ |
| 2 | Grid render (EN), images, Sale badge | ✅ Pass¹ |
| 3 | Currency on load + live USD/ILS switch (grid + cart drawer) | ✅ Pass |
| 4 | Card-click nav vs. Add-to-Cart drawer, active-currency line items | ✅ Pass |
| 5 | Hebrew (`/he`) names, "פריטים נבחרים", RTL, localized product links | ✅ Pass |
| 6 | Empty state omits the entire band | ✅ Pass |
| 7 | >8 note + homepage caps at 8 by featuredOrder | ✅ Pass (note tweak applied, see 7b) |

¹ The discounted-product / "Sale" badge path (steps 1–2) could not be exercised
because the admin only supports **batch** discounts today, not per-product — the
badge markup and logic are in place but were not visually confirmed against a live
discounted product. Captured as a note for follow-up.

## Punch list (deferred, non-blocking — phase still passes)

- **7b — reactive ">8" note (DONE this phase):** The soft amber note now recomputes on
  every featured-checkbox change in both add and edit forms, excluding the edited
  product from the base count, so it appears the instant a 9th product is featured.
  Shipped in commit `f966220` (folded into Plan 03's deliverable).
- **7a — batch featured editor (DEFERRED → backlog):** New feature to select many
  products and feature them in one action, with recommended bulk-action UX. Captured at
  `.planning/todos/pending/admin-batch-featured-editor.md`. Not a 40.1 gap — own design work.
- **Per-product discount (DEFERRED → noted in same todo):** Only batch discounts exist;
  blocks visual confirmation of the Sale badge on a single product.

## Self-Check: PASSED

No code changes in this plan beyond the small 7b note refinement (committed under
Plan 03's id). End-to-end dual-render flow (admin → schema → SSR → hydration →
currency → nav → empty state) confirmed working by the user.
