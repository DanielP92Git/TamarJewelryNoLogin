---
phase: 40.1-homepage-featured-products
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - admin/BisliView.js
autonomous: true
requirements: [CURR-03]
user_setup: []

must_haves:
  truths:
    - "The admin add-product and edit-product forms show a 'Featured on Homepage' checkbox"
    - "Checking the box reveals a Featured Order number input; the edit form pre-fills the saved values"
    - "Submitting the form sends isFeatured and featuredOrder to the product API"
    - "A soft, non-blocking note appears when more than 8 products are featured"
  artifacts:
    - path: "admin/BisliView.js"
      provides: "Homepage Featured card block in both forms + reveal toggle + soft count note + addProduct DOM reads"
      contains: "id=\"is-featured\""
  key_links:
    - from: "admin/BisliView.js addProduct() productData"
      to: "/addproduct + /updateproduct"
      via: "isFeatured + featuredOrder fields in the POST body"
      pattern: "isFeatured"
---

<objective>
Add the admin UX for featuring products (D-09, D-10): a "Featured on Homepage" checkbox + reveal-on-check "Featured Order" input in both the add-product and edit-product forms, the soft ">8 featured" note, and the `addProduct()` DOM reads that send `isFeatured`/`featuredOrder` to the product API.

Purpose: Without this, admins cannot flag products and the SSR grid (Plan 02) has nothing to render. Depends on Plan 01 (the API must accept these fields).
Output: Featured card block + interaction wiring + form-submission fields in `admin/BisliView.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-homepage-featured-products/40.1-CONTEXT.md
@.planning/phases/40.1-homepage-featured-products/40.1-PATTERNS.md
@.planning/phases/40.1-homepage-featured-products/40.1-UI-SPEC.md

<interfaces>
<!-- Existing BisliView structures to mirror. -->

admin/BisliView.js — two product form HTML strings:
  - EDIT form: bilingual name input `id="name-en"` at line ~4025 (template literal referencing `product.name_en`, `product.featuredOrder`, etc. are available on `product`).
  - ADD form: bilingual name input `id="name-en"` at line ~5855 (no product object — blank defaults).

admin/BisliView.js — addProduct(e, data, form) at line 5426; reads form values via `document.getElementById("name-en")?.value?.trim()` at line 5456; builds `productData` object at line 5603-5624; POSTs JSON to `/addproduct` (line 5633) with `Content-Type: application/json`.

admin/BisliView.js — existing `.card` markup convention (card > card__header > card__title; card__body > field > label/.label + input.input + .help) used throughout the forms (e.g. around lines 4015-4159).

Phase 01 contract: POST /addproduct + /updateproduct accept `isFeatured` (boolean/'true') and `featuredOrder` (number or null) in the JSON body; the server NaN-guards and auto-assigns order when blank.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Homepage Featured" card block to both product forms</name>
  <read_first>
    - admin/BisliView.js (read the EDIT form template around lines 4005-4160 and the ADD form template around lines 5840-5870 to find the insertion point after the Product Identifier card / before the Media card; read an existing `.card` block to match markup)
    - .planning/phases/40.1-homepage-featured-products/40.1-UI-SPEC.md (section "Admin Toggle — New UI Surface")
    - .planning/phases/40.1-homepage-featured-products/40.1-PATTERNS.md (section "admin/BisliView.js")
  </read_first>
  <action>
    Insert this `.card` block into BOTH the add-product and edit-product form HTML strings, placed after the "Product Identifier" card and before the right-column "Media" card. In the ADD form, omit the `product.*` interpolations (use unchecked / blank defaults); in the EDIT form, interpolate from `product`:
    ```html
    <div class="card">
      <div class="card__header"><h3 class="card__title">Homepage Featured</h3></div>
      <div class="card__body" style="display:flex; flex-direction:column; gap:16px;">
        <div class="field">
          <label style="display:flex; align-items:center; gap:8px; cursor:pointer;">
            <input type="checkbox" id="is-featured" name="isFeatured" ${product.isFeatured ? 'checked' : ''} />
            <span class="label" style="margin:0;">Featured on Homepage</span>
          </label>
          <div class="help">Show this product in the Featured Pieces grid on the homepage.</div>
        </div>
        <div class="field" id="featured-order-field" style="display:${product.isFeatured ? 'block' : 'none'};">
          <div class="label">Featured Order</div>
          <input class="input" type="number" id="featured-order" name="featuredOrder" value="${product.featuredOrder ?? ''}" min="1" step="1" style="width:100px;" />
          <div class="help">Lower numbers appear first. Leave blank to append to the end of the list.</div>
        </div>
        <div class="help" id="featured-count-note" style="display:none; color:#92400e; background:#fef3c7; border-radius:4px; padding:8px 16px;"></div>
      </div>
    </div>
    ```
    For the ADD form (no `product` in scope), hardcode the unchecked state: omit `checked`, set the order field to `style="display:none;"`, and `value=""`. Use only multiples-of-4 inline spacing (8px, 16px) per UI-SPEC — do not introduce other px values. The amber note colors (`#92400e`/`#fef3c7`) are the established warning convention, not new tokens.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'id="is-featured"' admin/BisliView.js | { read n; [ "$n" -ge 2 ] && echo "OK $n" || { echo "FAIL $n"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `admin/BisliView.js` contains `id="is-featured"` in 2 places (add form + edit form), `name="isFeatured"`
    - `admin/BisliView.js` contains `id="featured-order"` with `name="featuredOrder"` and `min="1"`
    - `admin/BisliView.js` contains `id="featured-order-field"` and `id="featured-count-note"`
    - The card title text `Homepage Featured` appears
    - The edit-form block interpolates `product.isFeatured` and `product.featuredOrder`
    - No inline px value other than 8px/16px (and the radius/padding in the note) is introduced
  </acceptance_criteria>
  <done>Both product forms render the Featured checkbox + (hidden-until-checked) order input + count-note container.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the reveal toggle, soft >8 note, and form-submission fields</name>
  <read_first>
    - admin/BisliView.js (read addProduct() at lines 5426-5644: the DOM reads at ~5456 and the productData object at ~5603-5624; read where other form interactions are wired after the form HTML is injected, e.g. the discount checkbox handler pattern)
    - .planning/phases/40.1-homepage-featured-products/40.1-PATTERNS.md (sections "Checkbox reveal interaction", "Soft note injection", "addProduct() DOM reads")
  </read_first>
  <action>
    In `admin/BisliView.js`:
    1. **Reveal toggle** — in the same init/script section where form interactions are bound (after the form HTML is rendered), add a change listener that mirrors the existing checkbox-toggle pattern:
       ```js
       document.getElementById('is-featured')?.addEventListener('change', function () {
         const f = document.getElementById('featured-order-field');
         if (f) f.style.display = this.checked ? 'block' : 'none';
       });
       ```
    2. **Soft note (D-10)** — after the form renders, fetch the featured count and show the note only when > 8:
       ```js
       fetch('/products?isFeatured=true')
         .then(r => r.json())
         .then(function (data) {
           const count = Array.isArray(data) ? data.filter(function (p) { return p.isFeatured; }).length : 0;
           if (count > 8) {
             const note = document.getElementById('featured-count-note');
             if (note) {
               note.textContent = count + ' products are featured — only the first 8 by Featured Order appear on the homepage.';
               note.style.display = 'block';
             }
           }
         })
         .catch(function () {}); // non-blocking
       ```
       (If `/products` is not a plain GET in this codebase, reuse whatever existing product-list fetch the dashboard already calls and filter client-side — do NOT add a new server route.)
    3. **Form submission** — in `addProduct()`, after the existing DOM reads (~line 5456), add:
       ```js
       const isFeatured = !!document.getElementById('is-featured')?.checked;
       const featuredOrderRaw = document.getElementById('featured-order')?.value;
       const featuredOrder = featuredOrderRaw ? parseInt(featuredOrderRaw, 10) : null;
       ```
       Then add `isFeatured` and `featuredOrder` to the `productData` object (around line 5603) so they are sent in the POST body. Ensure the edit-product submission path also includes these two fields in its request body (mirror wherever the edit form builds its update payload).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -Eq "isFeatured[,:]" admin/BisliView.js && grep -q "featured-order-field'\)\.style\.display\|getElementById('featured-order-field')" admin/BisliView.js && echo OK || { echo FAIL; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `admin/BisliView.js` contains a `change` listener on `is-featured` that sets `featured-order-field` display to `block`/`none`
    - `admin/BisliView.js` contains `document.getElementById('is-featured')?.checked` in addProduct()
    - `admin/BisliView.js` contains `parseInt(featuredOrderRaw, 10)` (or equivalent numeric read) for featuredOrder
    - The `productData` object includes `isFeatured` and `featuredOrder`
    - The edit/update payload also includes `isFeatured` and `featuredOrder`
    - The soft-note logic checks `count > 8` and sets `featured-count-note` display to `block`
  </acceptance_criteria>
  <done>The order field reveals on check, the >8 note appears when applicable, and both add and edit submissions carry isFeatured/featuredOrder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → product API | Admin form composes isFeatured/featuredOrder into the authenticated product POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-07 | Elevation of Privilege | featured write from admin UI | mitigate | The form POSTs to the existing `/addproduct` / `/updateproduct` endpoints which are already behind `requireAdmin` (Plan 01). This plan adds no new endpoint and changes no auth. |
| T-40.1-08 | Tampering | featuredOrder client input | mitigate | Client reads via `parseInt(...,10)` and sends `null` when blank; the server (Plan 01) is the authoritative NaN guard, so a hand-crafted body still cannot poison the sort. |
| T-40.1-09 | Information Disclosure | featured-count fetch | accept | The count fetch reuses the existing admin product-list data (already admin-scoped); no new data exposure. |
</threat_model>

<verification>
- Both forms render the Featured card; checkbox reveals the order field.
- Featuring a product via the form persists (round-trips through Plan 01 endpoints).
- The >8 note appears only when more than 8 products are featured.
- Admin lint/build unaffected (BisliView is a static admin script; load the admin page and confirm no console errors).
</verification>

<success_criteria>
- Products can be marked featured and toggled from the admin dashboard (Success Criterion 1 / D-09).
- Soft non-blocking >8 note implemented (D-10).
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-homepage-featured-products/40.1-03-SUMMARY.md`
</output>
