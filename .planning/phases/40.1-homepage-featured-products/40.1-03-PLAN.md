---
phase: 40.1-homepage-featured-products
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - admin/BisliView.js
autonomous: true
requirements: [CURR-03]
user_setup: []

must_haves:
  truths:
    - "The admin add-product and edit-product forms show a 'Featured on Homepage' checkbox"
    - "Checking the box reveals a Featured Order number input; the edit form pre-fills the saved values"
    - "Submitting EITHER the add OR the edit form sends isFeatured and featuredOrder to the product API"
    - "A soft, non-blocking note appears when more than 8 products are featured"
    - "Implements CONTEXT decisions D-09 (Featured-on-homepage checkbox + revealed featuredOrder input in add/edit forms) and D-10 (soft non-blocking note when >8 products featured)"
  artifacts:
    - path: "admin/BisliView.js"
      provides: "Homepage Featured card block in both forms + reveal toggle + soft count note + add/edit submission fields"
      contains: "id=\"is-featured\""
  key_links:
    - from: "admin/BisliView.js addProduct() productData (JSON)"
      to: "/addproduct"
      via: "isFeatured + featuredOrder fields in the JSON body"
      pattern: "isFeatured"
    - from: "admin/BisliView.js updateProduct() formData (multipart)"
      to: "/updateproduct/:id"
      via: "formData.append('isFeatured' ...) + formData.append('featuredOrder' ...)"
      pattern: "featuredOrder"
---

<objective>
Add the admin UX for featuring products (D-09, D-10): a "Featured on Homepage" checkbox + reveal-on-check "Featured Order" input in both the add-product and edit-product forms, the soft ">8 featured" note, and the submission wiring that sends `isFeatured`/`featuredOrder` to the product API on BOTH the add path (`addProduct()` → JSON → `/addproduct`) AND the edit path (`updateProduct()` → FormData → `/updateproduct/:id`).

Purpose: Without this, admins cannot flag products and the SSR grid (Plan 02) has nothing to render. Crucially, featuring-on-EDIT must persist — the edit form posts multipart FormData to `/updateproduct/:id`, so the two fields must be appended to that FormData, not just added to the add-path JSON. Depends on Plan 01 (the API must accept these fields).
Output: Featured card block + interaction wiring + add-path + edit-path submission fields in `admin/BisliView.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-homepage-featured-products/40.1-CONTEXT.md
@.planning/phases/40.1-homepage-featured-products/40.1-PATTERNS.md
@.planning/phases/40.1-homepage-featured-products/40.1-UI-SPEC.md

<interfaces>
<!-- Existing BisliView structures to mirror. Line numbers verified 2026-06-25. -->

admin/BisliView.js — two product form HTML strings:
  - EDIT form: built in `editProduct(product)` at line 3991; bilingual name input `id="name-en"` at line ~4025; `product` object is in scope (so `product.isFeatured` / `product.featuredOrder` are available for pre-fill).
  - ADD form: bilingual name input `id="name-en"` at line ~5855 (no product object — blank/unchecked defaults).

admin/BisliView.js — ADD submission: `addProduct(e, data, form)` at line 5426; reads DOM values via `document.getElementById("name-en")?.value?.trim()` at line 5456; builds the `productData` JSON object at lines 5603-5624; POSTs JSON (`Content-Type: application/json`) to `/addproduct` at line 5633.

admin/BisliView.js — EDIT submission: `updateProduct(e)` at line 4904; reads DOM values (~4935); builds a `FormData` object at line 4957 with `formData.append(...)` calls (lines 4958-4991); POSTs multipart to `` `${API_URL}/updateproduct/${productId}` `` at line 5004. THIS targets the `/updateproduct/:id` route Plan 01 wires — NOT the legacy `/updateproduct`. Because it is multipart, fields must be added via `formData.append("isFeatured", ...)` / `formData.append("featuredOrder", ...)`.

admin/BisliView.js — admin product list (data source for the >8 note): the dashboard already loads ALL products into `state.products`. Flow: `fetch(`${API_URL}/allproducts`)` at line 1453 → `loadProductsPage(data)` → `loadProducts(data)` which sets `state.products = data` at line 2992. So the featured count is `state.products.filter(p => p.isFeatured).length` — NO new fetch needed.

admin/BisliView.js — existing `.card` markup convention (card > card__header > card__title; card__body > field > label/.label + input.input + .help) used throughout the forms (e.g. around lines 4015-4159).

Phase 01 contract: POST /addproduct (JSON) + /updateproduct/:id (multipart) accept `isFeatured` (boolean or 'true'/'false' string) and `featuredOrder` (number, empty string, or null); the server NaN-guards and auto-assigns order when blank.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Homepage Featured" card block to both product forms</name>
  <read_first>
    - admin/BisliView.js (read the EDIT form template inside editProduct() around lines 3991-4160 and the ADD form template around lines 5840-5870 to find the insertion point after the Product Identifier card / before the Media card; read an existing `.card` block to match markup)
    - .planning/phases/40.1-homepage-featured-products/40.1-UI-SPEC.md (section "Admin Toggle — New UI Surface")
    - .planning/phases/40.1-homepage-featured-products/40.1-PATTERNS.md (section "admin/BisliView.js")
  </read_first>
  <action>
    Insert this `.card` block into BOTH the add-product and edit-product form HTML strings, placed after the "Product Identifier" card and before the right-column "Media" card. In the ADD form, omit the `product.*` interpolations (use unchecked / blank defaults); in the EDIT form (editProduct, `product` in scope), interpolate from `product`:
    ```html
    <div class="card">
      <div class="card__header"><h3 class="card__title">Homepage Featured</h3></div>
      <div class="card__body" style="display:flex; flex-direction:column; gap:16px;">
        <div class="field">
          <label style="display:flex; align-items:center; gap:8px; cursor:pointer;">
            <input type="checkbox" id="is-featured" name="isFeatured" ${product.isFeatured ? 'checked' : ''} />
            <span class="label" style="margin:0;">Featured on Homepage</span>
          </label>
          <div class="help">Show this product in the Featured Pieces grid on the homepage.</div>
        </div>
        <div class="field" id="featured-order-field" style="display:${product.isFeatured ? 'block' : 'none'};">
          <div class="label">Featured Order</div>
          <input class="input" type="number" id="featured-order" name="featuredOrder" value="${product.featuredOrder ?? ''}" min="1" step="1" style="width:100px;" />
          <div class="help">Lower numbers appear first. Leave blank to append to the end of the list.</div>
        </div>
        <div class="help" id="featured-count-note" style="display:none; color:#92400e; background:#fef3c7; border-radius:4px; padding:8px 16px;"></div>
      </div>
    </div>
    ```
    For the ADD form (no `product` in scope), hardcode the unchecked state: omit `checked`, set the order field to `style="display:none;"`, and `value=""`. Use only multiples-of-4 inline spacing (8px, 16px) per UI-SPEC — do not introduce other px values. The amber note colors (`#92400e`/`#fef3c7`) are the established warning convention, not new tokens.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'id="is-featured"' admin/BisliView.js | { read n; [ "$n" -ge 2 ] && echo "OK $n" || { echo "FAIL $n"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `admin/BisliView.js` contains `id="is-featured"` in 2 places (add form + edit form), `name="isFeatured"`
    - `admin/BisliView.js` contains `id="featured-order"` with `name="featuredOrder"` and `min="1"`
    - `admin/BisliView.js` contains `id="featured-order-field"` and `id="featured-count-note"`
    - The card title text `Homepage Featured` appears
    - The edit-form block (inside editProduct) interpolates `product.isFeatured` and `product.featuredOrder`
    - No inline px value other than 8px/16px (and the radius/padding in the note) is introduced
  </acceptance_criteria>
  <done>Both product forms render the Featured checkbox + (hidden-until-checked) order input + count-note container.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reveal toggle, soft >8 note (from state.products), and BOTH add + edit submission fields</name>
  <read_first>
    - admin/BisliView.js — ADD path: addProduct() at lines 5426-5644 (DOM reads ~5456; productData JSON at 5603-5624; POST to /addproduct at 5633)
    - admin/BisliView.js — EDIT path: updateProduct() at lines 4904-5010 (FormData created at 4957; append calls 4958-4991; POST to `${API_URL}/updateproduct/${productId}` at 5004) — THIS is where the edit-form featured fields must be appended
    - admin/BisliView.js — product-list state: loadProducts() sets `state.products = data` at line 2992 (loaded from /allproducts at line 1453) — the source for the featured count
    - admin/BisliView.js — read where other form interactions are wired after the form HTML is injected (e.g. the discount checkbox handler) to place the reveal listener consistently
    - .planning/phases/40.1-homepage-featured-products/40.1-PATTERNS.md (sections "Checkbox reveal interaction", "Soft note injection", "addProduct() DOM reads")
  </read_first>
  <action>
    In `admin/BisliView.js`:
    1. **Reveal toggle** — in the same init/script section where form interactions are bound (after the form HTML is rendered, for both add and edit forms), add a change listener mirroring the existing checkbox-toggle pattern:
       ```js
       document.getElementById('is-featured')?.addEventListener('change', function () {
         const f = document.getElementById('featured-order-field');
         if (f) f.style.display = this.checked ? 'block' : 'none';
       });
       ```
    2. **Soft note (D-10) — read from state.products (NO new fetch)** — after the form renders, compute the count from the already-loaded product list:
       ```js
       const featuredCount = Array.isArray(state.products)
         ? state.products.filter(function (p) { return p.isFeatured; }).length
         : 0;
       if (featuredCount > 8) {
         const note = document.getElementById('featured-count-note');
         if (note) {
           note.textContent = featuredCount + ' products are featured — only the first 8 by Featured Order appear on the homepage.';
           note.style.display = 'block';
         }
       }
       ```
       Do NOT add a new `/products?isFeatured=true` fetch — `state.products` (set at line 2992 from `/allproducts`) is the authoritative in-memory list. (If `state.products` is empty when the form opens, the note simply stays hidden — acceptable; the next list refresh re-evaluates.)
    3. **ADD submission** — in `addProduct()`, after the existing DOM reads (~line 5456), add:
       ```js
       const isFeatured = !!document.getElementById('is-featured')?.checked;
       const featuredOrderRaw = document.getElementById('featured-order')?.value;
       const featuredOrder = featuredOrderRaw ? parseInt(featuredOrderRaw, 10) : null;
       ```
       Then add `isFeatured` and `featuredOrder` to the `productData` JSON object (lines 5603-5624) so they are sent in the `/addproduct` POST body.
    4. **EDIT submission (CRITICAL — the under-specified path)** — in `updateProduct()`, alongside the existing `formData.append(...)` calls (lines 4958-4991, before the POST to `/updateproduct/${productId}` at 5004), read the same DOM fields and append them to the multipart FormData:
       ```js
       const editIsFeatured = !!document.getElementById('is-featured')?.checked;
       const editFeaturedOrderRaw = document.getElementById('featured-order')?.value;
       formData.append('isFeatured', editIsFeatured);
       formData.append('featuredOrder', editFeaturedOrderRaw ? parseInt(editFeaturedOrderRaw, 10) : '');
       ```
       (`formData.append` stringifies the boolean to `'true'`/`'false'`, which the Plan 01 update handler's `=== 'true'` check handles; empty string maps to null/auto-assign.) This guarantees featuring-on-EDIT persists via `/updateproduct/:id` — do NOT rely on the add path alone.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "formData.append('isFeatured'" admin/BisliView.js && grep -q "formData.append('featuredOrder'" admin/BisliView.js && grep -q "state.products" admin/BisliView.js && grep -q "getElementById('featured-order-field')" admin/BisliView.js && echo OK || { echo FAIL; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `admin/BisliView.js` contains a `change` listener on `is-featured` that sets `featured-order-field` display to `block`/`none`
    - ADD path: `addProduct()` reads `document.getElementById('is-featured')?.checked` and the `productData` JSON object includes `isFeatured` and `featuredOrder`
    - EDIT path: `updateProduct()` contains `formData.append('isFeatured'` AND `formData.append('featuredOrder'` (the multipart `/updateproduct/:id` payload carries both fields — NOT just the add path)
    - featuredOrder is read via `parseInt(...,10)` on both paths
    - The >8 note is computed from `state.products.filter(p => p.isFeatured).length` (no `fetch('/products?isFeatured=true')` and no other new network call)
    - The soft-note logic checks `> 8` and sets `featured-count-note` display to `block`
  </acceptance_criteria>
  <done>The order field reveals on check, the >8 note reads from state.products, and BOTH add (JSON) and edit (multipart FormData → /updateproduct/:id) submissions carry isFeatured/featuredOrder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → product API | Admin form composes isFeatured/featuredOrder into the authenticated product POST (JSON for add, multipart for edit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-07 | Elevation of Privilege | featured write from admin UI | mitigate | The forms POST to the existing `/addproduct` / `/updateproduct/:id` endpoints which are already behind `requireAdmin` (Plan 01). This plan adds no new endpoint and changes no auth; the legacy `/updateproduct` is not used by the edit form. |
| T-40.1-08 | Tampering | featuredOrder client input | mitigate | Client reads via `parseInt(...,10)` and sends `null`/empty when blank; the server (Plan 01) is the authoritative NaN guard, so a hand-crafted body still cannot poison the sort. |
| T-40.1-09 | Information Disclosure | featured-count source | accept | The count reads the already-loaded admin-scoped `state.products` array — no new data exposure and no new endpoint. |
</threat_model>

<verification>
- Both forms render the Featured card; checkbox reveals the order field.
- Featuring a product via the ADD form persists (round-trips through /addproduct).
- Featuring a product via the EDIT form persists (round-trips through /updateproduct/:id — verify the saved doc has isFeatured=true after an edit-form save).
- The >8 note appears only when more than 8 products are featured, sourced from state.products.
- Admin lint/build unaffected (BisliView is a static admin script; load the admin page and confirm no console errors).
</verification>

<success_criteria>
- Products can be marked featured and toggled from the admin dashboard via both add and edit forms (Success Criterion 1 / D-09).
- Soft non-blocking >8 note implemented from the existing product list (D-10).
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-homepage-featured-products/40.1-03-SUMMARY.md`
</output>
