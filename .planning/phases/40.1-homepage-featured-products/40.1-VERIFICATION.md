---
phase: 40.1-homepage-featured-products
verified: 2026-06-25T00:00:00Z
status: passed
score: 4/4 roadmap success criteria verified (all 19 plan-level must-have truths verified)
overrides_applied: 0
re_verification:
  previous_status: none
  note: "No prior VERIFICATION.md. A code REVIEW.md (issues_found) preceded this; its BLOCKER (CR-01) and WR-01 were fixed before verification."
---

# Phase 40.1: Homepage Featured Products Verification Report

**Phase Goal:** The homepage featured grid shows real, admin-curated products with correct dual-currency prices that re-price on currency change — replacing the hardcoded demo data in `homepage.js`
**Verified:** 2026-06-25
**Status:** passed
**Re-verification:** No — initial verification (a code review preceded it; its blocker was fixed)

## Goal Achievement

### Observable Truths — ROADMAP Success Criteria (the contract)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| SC1 | Products can be marked "featured" via `isFeatured` flag (Product schema) and toggled from the admin dashboard | VERIFIED | `Product.js:92-93` (`isFeatured`/`featuredOrder` paths), `BisliView.js:4168` (edit form checkbox) + `:6038` (add form checkbox); write path `products.js:322-323` (create), `:507-513` (update) behind existing `requireAdmin`. Node check: schema paths resolve `Boolean Number`. |
| SC2 | Homepage grid (`home.ejs #tk-prod-grid`) is SSR-rendered with real featured products and dual `usd_price`/`ils_price` in `data-*` attributes | VERIFIED | `ssr.js:158-166` queries `isFeatured:true`, `available:{$ne:false}`, `quantity:{$gt:0}`, `.sort({featuredOrder:1}).limit(8).select(...id...).lean()`; `home.ejs:188-196` emits `data-id`, `data-usd-price`, `data-ils-price`, `data-original-*`, `data-slug`, `data-name-en/he`. EJS compiles OK. |
| SC3 | `homepage.js` reads products + dual prices from SSR DOM (hardcoded `CURRENCY='ILS'` + demo `PRODUCTS` removed) and renders active currency from `localStorage.currency` on load | VERIFIED | `homepage.js:19` `getCurrency()` reads `localStorage`; grep confirms NO `var PRODUCTS`, NO `CURRENCY='ILS'`, NO grid `innerHTML=''`; `renderProducts()` (`:52-62`) hydrates `#tk-prod-grid .tk-prod` price-only. Syntax OK. |
| SC4 | Homepage grid (and cart drawer) re-prices immediately on `currency-changed`, consistent with category/cart | VERIFIED | `homepage.js:203-208` `currency-changed` listener validates enum + calls `renderProducts()` and `renderCart()`; cart stores dual `usd_price`/`ils_price` (`:80-81`) and re-computes per active currency (`:96,:109`). |

**Score:** 4/4 roadmap success criteria verified.

### Supporting Plan-Level Truths (all VERIFIED)

| Plan | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 01 | Featured flag persists in MongoDB | VERIFIED | schema + write path + admin UI all present |
| 01 | New featured product with no order auto-appends to end (global) | VERIFIED | `Product.js:158-178` pre-save hook, GLOBAL `findOne({isFeatured:true}).sort({featuredOrder:-1})`, NO category filter (D-01) |
| 01 | Featuring/un-featuring clears homepage SSR cache | VERIFIED | `invalidateHomePage()` called at `products.js:364` (create), `:702` (update), `:936-937` (legacy update, gated), `:1104-1105` (delete, gated) |
| 02 | Band omitted entirely when zero featured resolve (D-04) | VERIFIED | `home.ejs:139` `if (...length>0)` wraps whole `<section>`, closed `:213` |
| 02 | Sale badge only on discounted products (D-07) | VERIFIED | `home.ejs:199-201` `hasDiscount` guard on `tk-badge--gold` |
| 03 | Checkbox reveals order input; edit pre-fills saved values | VERIFIED | `BisliView.js:4173` reveal field with `product.featuredOrder ?? ''`; `:4515`/`:6246` change listeners |
| 03 | Both add (JSON) and edit (multipart) submissions carry the fields | VERIFIED | add `productData` `:5675-5676`; edit `formData.append('isFeatured'...)` `:5026-5027` → `/updateproduct/:id` |
| 03 | Soft non-blocking >8 note (D-10) from `state.products` | VERIFIED | `:4497-4506` (edit), `:6232-6237` (add); reactive recompute on checkbox change (7b, commit f966220) |
| 04 | Card click navigates to `/{lang}/product/{slug}` (D-06) | VERIFIED | `homepage.js:186-192` delegated click, skips `.tk-prod__add`, builds `/'+lang+'/product/'+slug` |
| 04 | Add-to-Cart stores both usd & ils prices (D-08) | VERIFIED | `homepage.js:80-81` pushes `usd_price`/`ils_price`; no single `price:` field |
| 05 | End-to-end flow (all 7 steps, /en + /he, populated/empty/>8/currency) | VERIFIED (human) | Plan 05 capstone APPROVED by user — `40.1-05-SUMMARY.md`; all 7 steps passed |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `backend/models/Product.js` | isFeatured + featuredOrder fields, auto-order hook, featured index | VERIFIED | fields `:92-93`, index `:126-127` `featured_order_idx`, global pre-save hook `:158-178` |
| `backend/cache/invalidation.js` | `invalidateHomePage()` deleting 4 home keys, exported | VERIFIED | `:100-104`, exported `:111`; `typeof === function` |
| `backend/routes/products.js` | create+update assign fields + invalidate home; legacy/delete also invalidate | VERIFIED | create `:322-323/:364`, update `:507-513/:702`, legacy `:936-937`, delete `:1104-1105` |
| `backend/routes/ssr.js` | renderHomePage featured query → home.ejs | VERIFIED | `:146-179`, async, empty-list fallback in catch |
| `backend/views/pages/home.ejs` | dual-price cards + empty-state guard | VERIFIED | `:139-213` |
| `admin/BisliView.js` | Featured card in both forms + reveal + note + submission | VERIFIED | 2× `id="is-featured"`; reveal/note/append all wired |
| `frontend/js/homepage.js` | SSR hydration, dual-currency, nav, dual-price cart | VERIFIED | demo data removed; hydration + listeners present |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| products.js write handlers | product.isFeatured/featuredOrder | req.body coercion behind requireAdmin | WIRED | NaN-safe `Number()` coercion; auth chain intact |
| products.js (all mutate paths) | invalidateHomePage() | post-save cache clear | WIRED | create/update unconditional; legacy/delete gated on isFeatured |
| ssr.js renderHomePage | home.ejs featuredProducts | pageData.featuredProducts | WIRED | assigned in try AND catch |
| home.ejs cards | homepage.js hydration | data-usd-price/data-ils-price/data-slug | WIRED | dataset read in renderProducts/addToCart |
| window currency-changed | renderProducts() + renderCart() | addEventListener | WIRED | `homepage.js:203-208` |
| card click | /{lang}/product/{slug} | delegated click reading data-slug | WIRED | `homepage.js:191` |
| admin add/edit forms | /addproduct + /updateproduct/:id | productData JSON + formData.append | WIRED | both paths send both fields |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| home.ejs `#tk-prod-grid` | `featuredProducts` | `Product.find({isFeatured:true,...}).lean()` in ssr.js | Yes — live Mongo query, real fields selected | FLOWING |
| homepage.js grid prices | `card.dataset.usdPrice/ilsPrice` | SSR `data-*` from DB query above | Yes — sourced from real product prices | FLOWING |
| homepage.js cart drawer | `cart[].usd_price/ils_price` | read from SSR card dataset on Add-to-Cart | Yes (homepage demo cart per D-08; real model.js cart deferred to Phase 43) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Product schema exposes featured paths | `node -e require('./models/Product')...` | `Boolean Number` | PASS |
| invalidateHomePage exported as function | `node -e require('./cache/invalidation')...` | `function` | PASS |
| home.ejs compiles | `ejs.compile(...)` | compiled, no error | PASS |
| homepage.js valid syntax | `node -c frontend/js/homepage.js` | OK | PASS |
| BisliView.js valid syntax | `node -c admin/BisliView.js` | OK | PASS |
| Live end-to-end (all 7 steps, /en + /he) | Plan 05 manual capstone | User APPROVED | PASS (human, already performed) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| CURR-03 | 40.1-01 → 05 | Home featured product grid prices respect `localStorage.currency` and update on currency change (remove hardcoded ILS in `homepage.js`) | SATISFIED | SC3 + SC4 verified; demo data removed; `currency-changed` re-pricing wired. REQUIREMENTS.md marks CURR-03 → Phase 40.1 → Complete. No orphaned IDs — CURR-03 is the sole declared requirement and is fully covered. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| home.ejs | 196 | `data-images` JSON emitted but never consumed by homepage.js (IN-01) | Info | Dead weight per card; no functional impact; deferred |
| products.js | 364, 702 | `invalidateHomePage()` fires on every add/update incl. non-featured (IN-02) | Info | Minor cache-hit erosion; safe/defensive; deferred |
| Product.js / ssr.js | hook + sort | `featuredOrder` non-unique; tie-break nondeterministic (IN-03) | Info | Order of tied items unstable; cosmetic; deferred |
| home.ejs / homepage.js | badge / cart name | Hebrew shows English "Sale" badge + English cart name (WR-02) | Warning (deferred) | i18n polish edge case; does not affect dual-currency goal |
| home.ejs / homepage.js | slug click | Empty-slug featured product is non-navigable (WR-03) | Warning (deferred) | Edge case (Hebrew-only-name products); click no-ops, no crash |

Note: REVIEW.md BLOCKER **CR-01** (delete path missing home cache invalidation) is **FIXED** — `products.js:1104-1105` (delete) and `:936-937` (legacy update) now call `invalidateHomePage()`. **WR-01** (cart drawer XSS via innerHTML) is **FIXED** — `homepage.js:112-132` now builds cart lines with `textContent`/`setAttribute` DOM nodes, not string-concatenated innerHTML.

### Deferred Items

These are documented and intentionally out of scope for this phase; they do not block the goal.

| # | Item | Disposition | Evidence |
| --- | ---- | ----------- | -------- |
| 1 | Sale-badge visual confirmation against a live single discounted product | Deferred — admin only supports batch discount today; badge markup + `hasDiscount` logic verified present in code | `40.1-05-SUMMARY.md` note ¹; todo `admin-batch-featured-editor.md` |
| 2 | Batch featured editor (bulk feature many products) | Deferred to backlog — new feature, own design work | `40.1-05-SUMMARY.md` 7a |
| 3 | WR-02 / WR-03 i18n edge cases (Hebrew "Sale" label, Hebrew cart name, empty-slug navigability) | Deferred — documented review warnings, not roadmap criteria | `40.1-REVIEW.md` WR-02/03 |

### Human Verification Required

None. The phase included an explicit human-verification capstone (Plan 05, `gate="blocking"`) covering all 7 end-to-end steps across `/en` and `/he` in populated, empty, currency-switched, and >8 states. The user performed this pass and replied "approved" (`40.1-05-SUMMARY.md`). Re-requesting human testing would duplicate an already-completed and approved gate. The only un-exercised visual (Sale badge on a per-product discount) is blocked by an admin limitation and is deferred to backlog, not a phase gap.

### Gaps Summary

No gaps. All 4 ROADMAP success criteria and all 19 plan-level must-have truths are verified against the codebase. The data layer (schema + global auto-order hook + index), admin-only write path, homepage cache invalidation (including the previously-missing delete/legacy paths), SSR featured query + dual-price card markup with empty-state omission, admin featuring UX (checkbox + reveal + >8 note + both submission paths), and homepage.js SSR hydration (demo data removed, dual-currency on-load + live re-pricing, card-click nav, dual-price cart drawer) are all present, substantive, wired, and flowing real data. The single requirement CURR-03 is satisfied. The code review's one blocker (CR-01) and the XSS warning (WR-01) were fixed prior to verification; remaining review findings are i18n/cosmetic and deferred with documentation.

---

_Verified: 2026-06-25_
_Verifier: Claude (gsd-verifier)_
