---
phase: 43-site-wide-cart-drawer
plan: 03
type: execute
wave: 2
depends_on: [43-01, 43-02]
files_modified:
  - frontend/js/View.js
autonomous: true
requirements: [D-03, D-05, D-06, D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - "D-05: the nav cart icon (#tk-cart-open) opens the drawer on EVERY page including /cart, with e.preventDefault() preserving its <a href='/{lang}/cart'> no-JS fallback"
    - "D-06: the drawer closes via the ✕ (#tk-cart-close), an overlay/scrim tap (#tk-overlay), and Esc — mirroring the Phase 42 three-way dismissal"
    - "D-09: drawer line prices + subtotal read the persisted currency (_getCurrentCurrency) and re-price on open and on every `currency-changed` event using per-item usd/ils prices — no hardcoded rate"
    - "D-07: each line has +/− quantity steppers and a remove control; changing them updates the line, the subtotal, and the #tk-cart-count badge live"
    - "D-08: rendering reuses the cartView currency/name helpers (no second money() impl) and editing calls the Plan 02 model mutators + existing removeFromUserCart"
    - "D-03: #tk-checkout is enabled only when the cart has items; D-10: empty cart shows #tk-cart-empty + Continue-shopping (#tk-cart-continue) which closes the drawer, and hides subtotal + checkout"
    - "Wiring is non-destructive (CLAUDE.md dual-render): View.js only binds handlers + builds line nodes inside #tk-drawer-body; it never innerHTML-rewrites the drawer chrome and survives the language toggle"
  artifacts:
    - path: "frontend/js/View.js"
      provides: "_bindCartDrawer + _renderCartDrawer + drawer currency helpers on the base View class"
      contains: "_bindCartDrawer"
  key_links:
    - from: "View.js _bindCartDrawer (#tk-cart-open click)"
      to: "drawer open + _renderCartDrawer()"
      via: "preventDefault + is-open toggle + render"
      pattern: "_renderCartDrawer"
    - from: "View.js _renderCartDrawer line +/− buttons"
      to: "model.increaseAmount / model.decreaseAmount / model.removeFromUserCart"
      via: "click handlers that mutate then re-render + persistCartNumber"
      pattern: "(increaseAmount|decreaseAmount|removeFromUserCart)"
    - from: "View.js currency-changed listener"
      to: "_renderCartDrawer()"
      via: "Phase 40 currency-changed subscriber"
      pattern: "currency-changed"
---

<objective>
Move the cart-drawer open/close + rendering off the home-only `homepage.js` demo and into the base `View.js` global chrome, wired to **real `model.cart` data**. After this plan, the nav cart icon opens a working mini-cart on **every** page (D-05), it renders real line items in the persisted currency (D-09), supports full in-drawer editing — quantity steppers + remove with live subtotal/badge (D-07/D-08), shows the empty state (D-10), enables checkout only when non-empty (D-03), and dismisses three ways (D-06). All via the non-destructive `hydratePrototypeChrome` bind-only pattern — no chrome `innerHTML` rewrites (CLAUDE.md).

Purpose: Deliver the site-wide working drawer against the Plan 01 markup + Plan 02 mutators.
Output: `_bindCartDrawer()`, `_renderCartDrawer()`, and the drawer currency/name helpers added to the base `View.js`, invoked from `hydratePrototypeChrome()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-site-wide-cart-drawer/43-CONTEXT.md
@.planning/phases/43-site-wide-cart-drawer/43-PATTERNS.md
@.planning/phases/40-currency-selector-wiring/40-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The structural twin to copy: _bindHamburgerMenu (View.js:888-976) -->
View.js already calls `this._bindHamburgerMenu()` from `hydratePrototypeChrome()` (View.js:847-886, the binder invoked on EVERY page). Add `this._bindCartDrawer()` at the same call site (after :879).

Non-destructive bind pattern + three-way dismissal (View.js:896-975), to mirror for the drawer:
```js
if (el.dataset.tkXBound === '1') return; el.dataset.tkXBound = '1';   // double-bind guard
const open = () => { overlay.classList.add('is-open'); document.body.style.overflow = 'hidden'; ... };
const close = () => { overlay.classList.remove('is-open'); document.body.style.overflow = ''; ... };
trigger.addEventListener('click', () => overlay.classList.contains('is-open') ? close() : open());
closeBtn.addEventListener('click', close);
overlay.addEventListener('click', e => { if (e.target.closest('a,button,select,input,label,[role="button"]')) return; close(); });
document.addEventListener('keydown', e => { if (e.key === 'Escape' && overlay.classList.contains('is-open')) close(); });
window.matchMedia('(min-width: 800px)').addEventListener('change', e => { if (e.matches && overlay.classList.contains('is-open')) close(); });
```

Badge sync helper (base View, ~:292): `this.persistCartNumber(num)` updates every `.cart-number-mobile` (incl. `#tk-cart-count`). `hydratePrototypeChrome` already calls it on load (:873).

Currency + name helpers to REPLICATE on base View (currently only on CartView, cartView.js:22-65 — base View instances like homePageView do NOT have them, so the drawer needs them on the base class):
```js
_getCurrentCurrency() { const s = localStorage.getItem('currency') || 'usd'; return s === 'ils' ? 'ils' : 'usd'; }
_getItemPrice(item, useOriginal=false) { /* usd: item.usdPrice||item.price ; ils: item.ilsPrice||item.price ; Math.round; original variants */ }
_getCurrencySymbol() { return this._getCurrentCurrency() === 'usd' ? '$' : '₪'; }
_getDrawerItemName(item) { const lng = localStorage.getItem('language')||'eng'; return lng==='heb' ? (item.name_he||item.name_en||item.title||'') : (item.name_en||item.title||''); }
```

model.js API (import * as model — View.js already imports model? verify; if not, import it):
```js
model.cart                  // array, source of truth
model.checkCartNumber()     // -> total count (sums amount)
model.increaseAmount(id)    // Plan 02 — returns new amount
model.decreaseAmount(id)    // Plan 02 — returns new amount
model.removeFromUserCart(id)// existing — removes the line
```

Plan 01 markup contract (in cart-drawer.ejs): `#tk-overlay`, `#tk-drawer`, `#tk-cart-open` (in header.ejs:50, an <a href="/{urlLang}/cart">), `#tk-cart-close`, `#tk-drawer-body`, `#tk-cart-empty`, `#tk-cart-continue`, `#tk-subtotal`, `#tk-checkout` (has `data-cart-url="/{urlLang}/cart"`). Line stepper class contract (CSS in Plan 01): `.tk-line__qty` row with `.tk-line__qty-btn` (− / +) + `.tk-line__qty-val`, and `.tk-line__remove`.

Demo line-builder to port to DOM-node style (homepage.js:108-145) — keep the `document.createElement` + `textContent`/`setAttribute` construction (XSS-safe, WR-01), but feed it `model.cart` + the helpers above, NOT the demo array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _bindCartDrawer() (open/close/dismissal) + drawer helpers to base View.js, invoked from hydratePrototypeChrome</name>
  <read_first>
    - frontend/js/View.js (lines 847-886 hydratePrototypeChrome; 888-976 _bindHamburgerMenu — the exact twin to copy; 292-324 persistCartNumber/increase/decrease)
    - frontend/js/Views/cartView.js (lines 22-65 the currency/name helpers to replicate on base View; 78-96 the currency-changed subscriber shape)
    - frontend/js/homepage.js (lines 147-154 openCart/closeCart, 213-215/239 the bindings being relocated)
    - .planning/phases/43-site-wide-cart-drawer/43-PATTERNS.md (View.js entry + Shared Pattern "Non-destructive SSR-chrome hydration")
  </read_first>
  <action>
    In `frontend/js/View.js`:

    1. Ensure `model` is imported (`import * as model from './model.js';`). If View.js does not already import model, add it; if a circular-import concern exists, import lazily inside the methods — but a top static import is preferred (cartView.js already does `import * as model from '../model.js'`).
    2. Add the four helpers from the interfaces block to the base View class IF not already present: `_getCurrentCurrency`, `_getItemPrice`, `_getCurrencySymbol`, `_getDrawerItemName` (copy the bodies from cartView.js:22-65; `_getDrawerItemName` mirrors cartView's `_getItemName`). These must live on the BASE class so non-cart pages (home, category, etc.) have them.
    3. Add `_bindCartDrawer()` as a near-copy of `_bindHamburgerMenu()`:
       - Elements: `const trigger = document.getElementById('tk-cart-open'); const overlay = document.getElementById('tk-overlay'); const drawer = document.getElementById('tk-drawer'); const closeBtn = document.getElementById('tk-cart-close');`
       - Guard: `if (!trigger || !overlay || !drawer) return;` then double-bind guard `if (trigger.dataset.tkCartBound === '1') return; trigger.dataset.tkCartBound = '1';`
       - `open()`: add `is-open` to BOTH `overlay` and `drawer` (the demo toggles both, homepage.js:148-149), `document.body.style.overflow = 'hidden'`, call `this._renderCartDrawer()` (Task 2) so it always shows current cart, and `requestAnimationFrame(() => closeBtn && closeBtn.focus())`.
       - `close()`: remove `is-open` from both, clear `document.body.style.overflow`, return focus to `trigger`.
       - D-05: bind `trigger.addEventListener('click', e => { e.preventDefault(); overlay.classList.contains('is-open') ? close() : open(); });` — the `preventDefault()` preserves the `<a href="/{urlLang}/cart">` fallback while opening the drawer on EVERY page incl. /cart (no page-specific no-op).
       - D-06 three-way dismissal: `closeBtn` click → close; `overlay` click → `if (e.target.closest('a, button, select, input, label, [role="button"]')) return; close();`; `document` keydown Escape → close.
       - DO NOT copy the hamburger's `matchMedia('(min-width: 800px)')` → close binding onto the drawer. That handler is correct for the mobile-only hamburger (no desktop equivalent), but the cart drawer is a valid feature at EVERY viewport width. Auto-closing it when the user crosses the 800px boundary (tablet rotate / window resize) while it is open would dismiss a legitimately-open drawer. The "clears the scroll lock" rationale does not apply — `close()` already clears `document.body.style.overflow`. The drawer must persist across the breakpoint; it is dismissed ONLY by the three explicit actions above (✕ / overlay / Esc) plus the cart-icon re-tap toggle.
       - D-10 empty-state action: bind `#tk-cart-continue` click → `close()` (the shopper keeps browsing wherever they are; no routing).
       - D-01/D-03 checkout nav: bind `#tk-checkout` click → if not disabled, navigate to its `data-cart-url` (`window.location.assign(checkoutBtn.getAttribute('data-cart-url'))`). Validate the path matches `^/(en|he)/cart$` before assigning (lang-injection guard); if absent, fall back to building it from the language map `{ eng:'en', heb:'he' }[localStorage.getItem('language')||'eng']`.
    4. In `hydratePrototypeChrome()` (after the `this._bindHamburgerMenu();` call at ~:879), add `this._bindCartDrawer();`. Do NOT rewrite any chrome innerHTML here (CLAUDE.md). The guard makes it survive the language toggle (the SSR drawer is never re-rendered).
  </action>
  <acceptance_criteria>
    - `grep -c '_bindCartDrawer' frontend/js/View.js` >= 2 (definition + call in hydratePrototypeChrome).
    - View.js contains `getElementById('tk-cart-open')`, `getElementById('tk-cart-close')`, `getElementById('tk-overlay')`, `getElementById('tk-drawer')`.
    - `_bindCartDrawer` contains `e.preventDefault()` on the `#tk-cart-open` handler (D-05 fallback preserved) and an `Escape` keydown close (D-06).
    - View.js contains a `data-cart-url` read and a `/(en|he)/cart` validation (lang-injection guard).
    - `grep -c 'tkCartBound' frontend/js/View.js` >= 1 (double-bind guard).
    - `_bindCartDrawer` does NOT register a `matchMedia('(min-width: 800px)')` → close handler (the drawer persists across the breakpoint, unlike the hamburger). Verify the drawer's close paths are only ✕ / overlay / Esc / icon-toggle.
    - `cd frontend && npm run build` succeeds; backend restarted to pick up the new bundle hash (note in summary).
  </acceptance_criteria>
  <verify>
    <automated>grep -c '_bindCartDrawer' frontend/js/View.js | grep -qv '^[01]$' && grep -q "getElementById('tk-cart-open')" frontend/js/View.js && grep -q "tkCartBound" frontend/js/View.js && echo OK</automated>
  </verify>
  <done>The drawer opens/closes (D-05/D-06) on every page from base View.js with a non-destructive double-bind guard; checkout nav is lang-validated; helpers exist on the base class.</done>
</task>

<task type="auto">
  <name>Task 2: Add _renderCartDrawer() — real cart lines, currency, subtotal, badge, empty state, checkout toggle</name>
  <read_first>
    - frontend/js/homepage.js (lines 91-146 the demo renderCart + DOM-node line builder to port; lines 99-101 the subtotal/badge/checkout-disabled toggles)
    - frontend/js/Views/cartView.js (lines 205-272 the real per-item markup using _getItemPrice/_getCurrencySymbol/_getItemName, item.amount; the source of the pricing logic to mirror)
    - frontend/js/model.js (cart shape; checkCartNumber sums amount)
    - .planning/phases/43-site-wide-cart-drawer/43-PATTERNS.md (cartView entry — "DOM-node construction style for the drawer")
  </read_first>
  <action>
    Add `_renderCartDrawer()` to base View.js. It reads `model.cart` (NOT the demo array) and rebuilds ONLY the line nodes inside `#tk-drawer-body` (never the drawer chrome):

    1. `const body = document.getElementById('tk-drawer-body'); if (!body) return;` and grab `#tk-cart-empty`, `#tk-subtotal`, `#tk-checkout`, the `.tk-drawer__foot`.
    2. Clear previously-injected lines only: remove existing `.tk-line` nodes from `body` (do NOT remove `#tk-cart-empty`).
    3. Empty state (D-10): if `model.cart.length === 0` → show `#tk-cart-empty` (remove its `hidden`), hide the `.tk-drawer__foot` (subtotal + checkout) e.g. via a `hidden` attr or a `is-empty` class on the drawer, set `#tk-checkout.disabled = true` (D-03), set `#tk-subtotal` to the zero value `this._getCurrencySymbol() + '0'`, update badge `this.persistCartNumber(0)`, and return.
    4. Non-empty: hide `#tk-cart-empty` (set `hidden`), show `.tk-drawer__foot`, enable `#tk-checkout` (`disabled = false`, D-03). For each `item` in `model.cart` build a `.tk-line` with DOM nodes (XSS-safe — `textContent`/`setAttribute`, NEVER `innerHTML` for product-sourced data, WR-01 / threat T-43-01):
       - `.tk-line__media > img` with `img.setAttribute('src', item.image || '')` + `img.setAttribute('alt', this._getDrawerItemName(item))` (NO `crossorigin` attr — CLAUDE.md CORS rule).
       - `.tk-line__main` containing `.tk-line__row` (`.tk-line__name` = `this._getDrawerItemName(item)` via textContent; `.tk-line__total` = `this._getCurrencySymbol() + this._getItemPrice(item) * (Number(item.amount)||1)`).
       - `.tk-line__meta` containing the D-07 stepper `.tk-line__qty` row: a `.tk-line__qty-btn` "−" button, a `.tk-line__qty-val` (textContent = `item.amount`), a `.tk-line__qty-btn` "+" button; plus a `.tk-line__remove` button (label "Remove" / "הסר" per `localStorage.language`). Set `data-id="item.id"` on the line or buttons so Task 3 handlers can resolve the item.
       - Append before/after the `#tk-cart-empty` node (which stays hidden) so the contract holds.
    5. Subtotal (D-09): `const subtotal = model.cart.reduce((s,it)=> s + this._getItemPrice(it) * (Number(it.amount)||1), 0); document.getElementById('tk-subtotal').textContent = this._getCurrencySymbol() + subtotal;` — currency via the helpers, never a hardcoded 3.7 rate.
    6. Badge: `this.persistCartNumber(model.cart.reduce((s,it)=> s + (Number(it.amount)||1), 0))` (or `await model.checkCartNumber()`).
    7. Currency subscriber (D-09): once (idempotent guard `if (!this._drawerCurrencyBound)`), `window.addEventListener('currency-changed', e => { const c = e?.detail?.currency; if (c!=='usd'&&c!=='ils') return; if (document.getElementById('tk-overlay')?.classList.contains('is-open')) this._renderCartDrawer(); else this._renderCartDrawer(); });` — re-price whether open or closed (mirrors cartView.js:78-96; keep a try/catch with no destructive reload needed since we only touch the body).
    8. Call `_renderCartDrawer()` from `_bindCartDrawer`'s `open()` (Task 1) and register the currency subscriber inside `_bindCartDrawer` so it is wired once per page.
  </action>
  <acceptance_criteria>
    - `grep -c '_renderCartDrawer' frontend/js/View.js` >= 2 (definition + at least one call from open()/currency listener).
    - `_renderCartDrawer` references `model.cart` and uses `this._getItemPrice` + `this._getCurrencySymbol` (no literal `3.7` or `money(` in View.js).
    - Line construction uses `textContent`/`createElement` for names; `grep -c 'innerHTML' ` added lines == 0 for product-name nodes (no `innerHTML` with item fields).
    - View.js contains `tk-cart-empty` handling and toggles `#tk-checkout` `disabled` based on `model.cart.length`.
    - View.js registers a `currency-changed` listener that calls `_renderCartDrawer`.
    - `cd frontend && npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '_renderCartDrawer' frontend/js/View.js | grep -qv '^[01]$' && grep -q 'model.cart' frontend/js/View.js && grep -q 'currency-changed' frontend/js/View.js && ! grep -q '3.7' frontend/js/View.js && echo OK</automated>
  </verify>
  <done>Opening the drawer renders real model.cart lines in the persisted currency with live subtotal + badge, an empty state, and a checkout button enabled only when non-empty; it re-prices on currency-changed.</done>
</task>

<task type="auto">
  <name>Task 3: Wire in-drawer editing — quantity steppers + remove → model mutators with live re-render (D-07/D-08)</name>
  <read_first>
    - frontend/js/View.js (the _renderCartDrawer line builder from Task 2 — the buttons to bind)
    - frontend/js/model.js (increaseAmount/decreaseAmount from Plan 02; removeFromUserCart existing)
    - frontend/js/Views/cartView.js (lines 36-65 the shared price helpers; the cart-item id convention)
    - .planning/phases/43-site-wide-cart-drawer/43-PATTERNS.md (D-07/D-08 "reuse model.js mutators")
  </read_first>
  <action>
    In `_renderCartDrawer()` (View.js), attach handlers to each line's controls (D-07 full editing; D-08 reuse the model mutators — do NOT reimplement min/stock logic here, it lives in Plan 02):

    - "+" button → `await model.increaseAmount(item.id); this._renderCartDrawer();`
    - "−" button → `await model.decreaseAmount(item.id); this._renderCartDrawer();`
    - `.tk-line__remove` → `await model.removeFromUserCart(item.id); this._renderCartDrawer();`

    After each mutation the re-render recomputes the line total, the `#tk-subtotal`, the `#tk-cart-count` badge (via `persistCartNumber`), and flips to the empty state when the last line is removed (Task 2 logic) — giving live subtotal + badge updates per D-07. Because all mutators operate on the shared `model.cart`, the /cart page (if open/visited next) reflects the same state (D-08 "shared model state"). Prefer event delegation on `#tk-drawer-body` (one listener that reads `e.target.closest('.tk-line__qty-btn--plus'/'--minus'/'.tk-line__remove')` and the line's `data-id`) over per-node listeners so re-renders don't leak handlers; if you keep per-node listeners, rebuild them on each render (the body is cleared each time, so no double-binding).

    CLASS CONTRACT (must match Plan 01 exactly): each stepper button MUST carry BOTH classes on the same element — the base `tk-line__qty-btn` (so Plan 01's CSS styles it) AND its modifier (so the JS delegation selector binds it). Build them as `<button class="tk-line__qty-btn tk-line__qty-btn--minus" ...>` and `<button class="tk-line__qty-btn tk-line__qty-btn--plus" ...>`. The delegation reads `e.target.closest('.tk-line__qty-btn--plus')` / `'.tk-line__qty-btn--minus')`. A button missing the base class renders unstyled; a button missing the modifier never binds — both classes are required on every stepper button.
  </action>
  <acceptance_criteria>
    - View.js calls `model.increaseAmount`, `model.decreaseAmount`, and `model.removeFromUserCart` from within the drawer rendering/handlers.
    - Stepper buttons carry BOTH the base `tk-line__qty-btn` class AND the `--plus`/`--minus` modifier on the same element (e.g. `class="tk-line__qty-btn tk-line__qty-btn--plus"`) — verify `grep -q 'tk-line__qty-btn tk-line__qty-btn--plus' frontend/js/View.js` and the `--minus` equivalent (or that both class tokens are set on each button node).
    - Each handler calls `this._renderCartDrawer()` after the mutation (live update).
    - No min-1 / stock-cap logic duplicated in View.js (those live only in model.js Plan 02) — `grep` View.js shows no `<= 1` quantity floor.
    - `cd frontend && npm run build` succeeds; backend restarted (note in summary).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'increaseAmount' frontend/js/View.js && grep -q 'decreaseAmount' frontend/js/View.js && grep -q 'removeFromUserCart' frontend/js/View.js && grep -q 'tk-line__qty-btn--plus' frontend/js/View.js && echo OK</automated>
  </verify>
  <done>Drawer lines have working +/−/remove that mutate the shared cart via the model API and live-update subtotal + badge + empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-authored product fields → drawer DOM | Product names/images/prices from `model.cart` are injected into `#tk-drawer-body` on the client. |
| URL language segment → checkout navigation | `#tk-checkout` navigates to a `/{lang}/cart` path read from `data-cart-url` / built from the language map. |
| DOM dataset id → model mutation | Stepper/remove handlers pass a line `data-id` into model mutators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering/Injection (XSS) | `#tk-drawer-body` line rendering | mitigate | All product-sourced fields (name, image src/alt) are written with `textContent`/`setAttribute` on `createElement` nodes — NEVER `innerHTML` interpolation. Prices are numbers from `_getItemPrice` (Math.round). Verified by the "no innerHTML with item fields" acceptance check. |
| T-43-02 | Tampering (open redirect / lang injection) | `#tk-checkout` navigation | mitigate | The `data-cart-url` value is validated against `^/(en|he)/cart$` before `window.location.assign`; the fallback builds the path from a fixed `{eng:'en',heb:'he'}` map, so no attacker-influenced language string reaches the navigation. |
| T-43-07 | Tampering | quantity edits via steppers | mitigate | Steppers call the Plan 02 mutators which floor at 1 and cap at stock; View.js adds no quantity it does not clamp. Server re-validates totals at checkout (existing /cart flow). |
| T-43-08 | Repudiation/Integrity | shared model.cart state | accept | Drawer and /cart page share one in-memory cart; this is the intended single source of truth, re-validated server-side at payment. |
</threat_model>

<verification>
- Drawer opens on home, category, product, cart, about, contact (D-05) and closes via ✕/overlay/Esc (D-06).
- Lines render from real model.cart in the persisted currency; subtotal + badge live-update; re-prices on currency-changed (D-09).
- +/−/remove edit the shared cart via model mutators (D-07/D-08); checkout enabled only when non-empty (D-03); empty state shows Continue-shopping that closes the drawer (D-10).
- Survives the in-page language toggle (double-bind guard; no chrome innerHTML rewrite).
- Frontend JS change → `npm run build` in /frontend + backend restart required.
</verification>

<success_criteria>
- A fully working site-wide mini-cart bound from base View.js against the Plan 01 markup + Plan 02 mutators, honoring D-03, D-05, D-06, D-07, D-08, D-09, D-10 and the CLAUDE.md dual-render rule.
</success_criteria>

<output>
After completion, create `.planning/phases/43-site-wide-cart-drawer/43-03-SUMMARY.md`.
</output>
