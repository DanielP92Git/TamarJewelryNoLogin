---
phase: 41-footer-social-restore
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - frontend/js/View.js
  - frontend/js/Views/aboutView.js
  - frontend/js/Views/contactMeView.js
  - frontend/js/Views/homePageView.js
  - frontend/js/Views/policiesView.js
  - frontend/tests/views/home.test.js
  - CLAUDE.md
autonomous: true
requirements: [FOOT-03]

must_haves:
  truths:
    - "No dead footer JS remains: setFooterLng and handleFooterMarkup are fully removed from View.js"
    - "No subclass calls a removed footer method (no TypeError on language toggle on home/about/contact/policies pages)"
    - "Language toggle on every page still works after the JS retirement (footer stays SSR-static, untouched by JS)"
    - "Frontend test suite passes with the home.test.js footer mock/assertion removed"
  artifacts:
    - path: "frontend/js/View.js"
      provides: "Base View class with the dead footer code removed"
      absent: "setFooterLng / handleFooterMarkup"
  key_links:
    - from: "frontend/js/Views/{aboutView,contactMeView,homePageView,policiesView}.js"
      to: "removed base methods"
      via: "call sites deleted so no reference survives"
      pattern: "setFooterLng|handleFooterMarkup"
---

<objective>
Retire the now-dead legacy footer JS so the footer is SSR-static (Plan 01 ported the
social markup into the SSR partial). Remove `setFooterLng()` + `handleFooterMarkup()`
from the base `View.js`, AND remove every live call site in the subclasses, AND update
the test that mocks/asserts the removed method.

Purpose: D-08 — the footer no longer needs a JS twin. CRITICAL: a codebase grep found
that `setFooterLng` / `handleFooterMarkup` are called from FIVE additional files beyond
the single L928 call site CONTEXT.md anticipated. Deleting the base methods without
removing these call sites would throw `TypeError: this.setFooterLng is not a function`
on the in-page language toggle of the home, about, contact, and policies pages.
Output: 6 files updated; no surviving reference to either method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-footer-social-restore/41-CONTEXT.md
@.planning/phases/41-footer-social-restore/41-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Complete set of references found by `grep -rn 'setFooterLng|handleFooterMarkup' frontend/` -->
<!-- DEFINITIONS (to delete) — frontend/js/View.js:
       L928   this.handleFooterMarkup(lng);   (call site inside setLngElements)
       L1073-1199  setFooterLng(lng) { ... }   (EN/HE legacy template-string footer)
       L1201-1206  handleFooterMarkup(lng) { ... } (targets dead '.footer' selector)
-->
<!-- CALL SITES (to delete) in subclasses:
       aboutView.js:98   this.setFooterLng('heb');   (in changeToHeb)
       aboutView.js:105  this.setFooterLng('eng');   (in changeToEng)
       contactMeView.js:191  this.setFooterLng('heb');
       contactMeView.js:199  this.setFooterLng('eng');
       homePageView.js:56   this.setFooterLng('heb');   (in changeToHeb)
       homePageView.js:69   this.setFooterLng('eng');   (in changeToEng)
       homePageView.js:139  this.handleFooterMarkup(lng);  (in setHomeLanguage)
       policiesView.js:10   this.setFooterLng('heb');
       policiesView.js:20   this.setFooterLng('eng');
-->
<!-- TEST (to update) — frontend/tests/views/home.test.js:
       L95-96  vi.spyOn(homePageView, 'handleFooterMarkup').mockImplementation(...)
       L111-112  expect(handleFooterMock).toHaveBeenCalled();
   The test 'should set page-specific language via setPageSpecificLanguage' must keep
   its category-language assertions; only the footer mock + footer assertion are removed.
-->
<!-- All discarded call results: subclasses call setFooterLng and throw away the return
     string (it was never appended anywhere), and handleFooterMarkup is a no-op because
     it targets '.footer' which no SSR view renders. Removal cannot regress rendering. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete setFooterLng + handleFooterMarkup definitions and the L928 call from View.js</name>
  <files>frontend/js/View.js</files>
  <read_first>
    - frontend/js/View.js L920-940 (the `handleFooterMarkup(lng)` call site at L928 inside setLngElements) and L1070-1210 (the `setFooterLng` + `handleFooterMarkup` method definitions)
    - .planning/phases/41-footer-social-restore/41-PATTERNS.md (§View.js — RETIRE dead footer JS, L112-143)
    - .planning/phases/41-footer-social-restore/41-CONTEXT.md (D-08)
  </read_first>
  <action>
    In frontend/js/View.js remove THREE things:
    1. The call site at ~L927-928 — delete the line `this.handleFooterMarkup(lng);` and
       its preceding comment `// Update footer and handle other elements`.
    2. The entire `setFooterLng(lng) { ... }` method (~L1073-1199) — the big EN/HE legacy
       template-string footer.
    3. The entire `handleFooterMarkup(lng) { ... }` method (~L1201-1206).
    Leave the surrounding methods intact (the language-button listeners after L928 and
    `_generateCategoriesListMarkup` after L1206 must remain). Match braces carefully so
    the class body stays balanced.
  </action>
  <acceptance_criteria>
    - `grep -c 'setFooterLng' frontend/js/View.js` == 0
    - `grep -c 'handleFooterMarkup' frontend/js/View.js` == 0
    - View.js still contains `_generateCategoriesListMarkup` (surrounding method preserved)
    - View.js still contains the `.heb-lng` / `.eng-lng` language-button listeners (the block formerly after the deleted L928 call)
    - File parses: `cd frontend && npx eslint js/View.js` reports no parse/syntax error (brace balance intact)
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'setFooterLng\|handleFooterMarkup' frontend/js/View.js)" = "0" ] && cd frontend && npx eslint js/View.js</automated>
  </verify>
  <done>View.js contains zero references to setFooterLng/handleFooterMarkup; surrounding methods intact; lints without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all subclass call sites and update home.test.js</name>
  <files>frontend/js/Views/aboutView.js, frontend/js/Views/contactMeView.js, frontend/js/Views/homePageView.js, frontend/js/Views/policiesView.js, frontend/tests/views/home.test.js</files>
  <read_first>
    - frontend/js/Views/aboutView.js L94-106 (changeToHeb/changeToEng)
    - frontend/js/Views/contactMeView.js L187-201 (changeToHeb/changeToEng)
    - frontend/js/Views/homePageView.js L50-74 (changeToHeb/changeToEng) and L136-140 (setHomeLanguage)
    - frontend/js/Views/policiesView.js L4-22 (changeToHeb/changeToEng)
    - frontend/tests/views/home.test.js L94-113 (the 'setPageSpecificLanguage' test)
    - .planning/phases/41-footer-social-restore/41-PATTERNS.md (§View.js retire note — "grep for any OTHER references before deleting")
  </read_first>
  <action>
    Delete every call to the now-removed methods. Each is a standalone statement whose
    return value was discarded, so deletion is safe and changes no other behavior:
    - aboutView.js: remove `this.setFooterLng('heb');` (L98) and `this.setFooterLng('eng');` (L105).
    - contactMeView.js: remove `this.setFooterLng('heb');` (L191) and `this.setFooterLng('eng');` (L199).
    - homePageView.js: remove `this.setFooterLng('heb');` (L56), `this.setFooterLng('eng');` (L69),
      AND `this.handleFooterMarkup(lng);` (L139) inside `setHomeLanguage` (leave the
      `this.setCategoriesLng(lng);` line above it — that is the real work `setHomeLanguage` does).
    - policiesView.js: remove `this.setFooterLng('heb');` (L10) and `this.setFooterLng('eng');` (L20).

    In frontend/tests/views/home.test.js, the test 'should set page-specific language via
    setPageSpecificLanguage' (L94-113):
    - remove the mock at L95-96 (`const handleFooterMock = vi.spyOn(homePageView, 'handleFooterMarkup')...`)
      and its comment.
    - remove the assertion at L111-112 (`// Verify handleFooterMarkup was called` +
      `expect(handleFooterMock).toHaveBeenCalled();`).
    - KEEP the category-language assertions (the `Necklaces` / `שרשראות` checks) — those
      are the meaningful part of the test and must still pass.

    Do NOT touch any other call inside these methods (setLanguage, setAboutDesc,
    setFormLng, setPoliciesContent, setCategoriesLng, super.changeTo*, _updateUrlLang,
    ensureCartIconVisibility all remain).
  </action>
  <acceptance_criteria>
    - `grep -rc 'setFooterLng\|handleFooterMarkup' frontend/js/Views/` reports 0 across all four view files
    - `grep -c 'handleFooterMarkup\|setFooterLng' frontend/tests/views/home.test.js` == 0
    - homePageView.js `setHomeLanguage` still contains `this.setCategoriesLng(lng);`
    - aboutView.js changeToHeb still contains `this.setAboutDesc(`; contactMeView.js still contains `this.setFormLng(`; policiesView.js still contains `this.setPoliciesContent(`
    - whole-repo check: `grep -rc 'setFooterLng\|handleFooterMarkup' frontend/js frontend/tests` == 0 (combined with Task 1's View.js cleanup)
    - `cd frontend && npm test` passes (home.test.js green, no TypeError)
  </acceptance_criteria>
  <verify>
    <automated>[ -z "$(grep -rl 'setFooterLng\|handleFooterMarkup' frontend/js frontend/tests)" ] && cd frontend && npm test</automated>
  </verify>
  <done>No file under frontend/ references setFooterLng/handleFooterMarkup; subclass language toggles and the home test still work; test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md so the dual-render map reflects the SSR-only footer</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md §"SSR + Client Dual-Render" (L90-101) — specifically the Footer bullet at L97 of the "Page ↔ View ↔ overwrite map"
    - .planning/phases/41-footer-social-restore/41-CONTEXT.md (D-08 — the footer becomes SSR-static after this phase)
    - frontend/js/View.js (confirm Tasks 1-2 already removed the footer JS before editing the doc, so the doc matches reality)
  </read_first>
  <action>
    Run this AFTER Tasks 1-2 (the JS deletion) so the doc matches the code. The current
    Footer bullet (CLAUDE.md L97) is now FALSE — it describes a JS twin that no longer
    exists:
    `- **Footer** (\`backend/views/partials/footer.ejs\`) ↔ \`frontend/js/View.js\` → \`handleFooterMarkup()\` runs \`footer.innerHTML = setFooterLng(lng)\` on every page. Social links / footer markup live in **both** places.`

    Replace that entire bullet line with the following (note: it must NOT name the retired
    methods literally, so the grep gate below stays clean):
    `- **Footer** (\`backend/views/partials/footer.ejs\`) — **SSR-only.** Its old JS twin (the footer language-rewrite methods in \`View.js\`) was retired in Phase 41; social links and all footer markup now live only in \`footer.ejs\`. The dual-render trap no longer applies to the footer — edit the EJS partial directly.`

    Do NOT alter the other bullets (About, Workshop, Contact, Policies) or the surrounding
    prose. Leaving the historical "This has already bitten the footer…" sentence at L92 is
    fine — it is a past-tense statement and remains accurate.
  </action>
  <acceptance_criteria>
    - `grep -c 'handleFooterMarkup' CLAUDE.md` == 0
    - `grep -c 'setFooterLng' CLAUDE.md` == 0
    - CLAUDE.md contains the string `SSR-only` in the Footer bullet
    - CLAUDE.md contains the phrase `retired in Phase 41`
    - CLAUDE.md still contains the other overwrite-map bullets: `setAboutDesc`, `setFormLng`, `setPoliciesContent` (untouched)
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'handleFooterMarkup\|setFooterLng' CLAUDE.md)" = "0" ] && grep -q 'SSR-only' CLAUDE.md && grep -q 'retired in Phase 41' CLAUDE.md && grep -q 'setPoliciesContent' CLAUDE.md && echo PASS</automated>
  </verify>
  <done>CLAUDE.md's dual-render Footer bullet states the footer is SSR-only with its JS twin retired in Phase 41; no reference to the removed methods remains; other bullets untouched.</done>
</task>

</tasks>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure dead-code removal in client JS + a test edit. No data, no inputs, no new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Denial of Service (self-inflicted runtime error) | subclass language-toggle handlers | mitigate | Removing base methods without their call sites would throw `TypeError` on language toggle, breaking the page. Mitigated by deleting ALL nine call sites (this plan's Task 2) and gating on `npm test` + a whole-repo grep == 0. |
| T-41-05 | Tampering (regression) | footer rendering | accept | `handleFooterMarkup` targeted `.footer`, which no SSR view renders, so it was a confirmed no-op; `setFooterLng` return values were discarded. Removal cannot change rendered output — the SSR footer (Plan 01) is the sole source. Verified by the Plan 01 checkpoint on /en, /he, /en/about. |
</threat_model>

<verification>
- Whole-repo grep: `grep -rl 'setFooterLng\|handleFooterMarkup' frontend/` returns nothing
- `cd frontend && npm test` passes (including the amended home.test.js)
- `cd frontend && npm run build` exits 0 (no broken imports/references)
- Manual sanity (optional): toggle language on /en/about and /en/contact — no console TypeError, footer unchanged (SSR-static)
</verification>

<success_criteria>
- FOOT-03: footer is SSR-static and renders consistently — no dead JS path can interfere
  with or overwrite the social column on any page
- Zero references to setFooterLng/handleFooterMarkup remain anywhere under frontend/
- Test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/41-footer-social-restore/41-02-SUMMARY.md`
</output>
