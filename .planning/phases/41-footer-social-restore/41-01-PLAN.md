---
phase: 41-footer-social-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/views/partials/footer.ejs
  - frontend/css/homepage.css
autonomous: false
requirements: [FOOT-01, FOOT-02, FOOT-03]

must_haves:
  truths:
    - "Visitor sees Instagram + Facebook icons under a 'Follow Me' heading as the 4th footer column on every page"
    - "Clicking Instagram opens https://www.instagram.com/tamar_kfir_jewelry/ in a new tab"
    - "Clicking Facebook opens https://www.facebook.com/tamarkfirjewelry in a new tab"
    - "On /he the social heading reads 'עקבו אחרי' and the 4-column grid mirrors naturally in RTL"
    - "The 4-column footer reflows to 2x2 (not a 4-col cram) at the 860px mobile breakpoint"
    - "The social column renders identically on a non-home SSR page (e.g. /en/about)"
    - "D-01: social section is the 4th column in .tk-footer__cols (grid repeat(3,1fr) -> repeat(4,1fr)), under a heading, not in .tk-footer__bar"
    - "D-02: 4-column grid reflows sanely (2x2) at the mobile breakpoint, no 4-col cram at the 760px max-width"
    - "D-03: social column heading uses .tk-footer__heading, EN 'Follow Me' / HE 'עקבו אחרי'"
    - "D-04: Instagram (outline) + Facebook (solid) inline SVGs ported from View.js setFooterLng() into footer.ejs, renamed to tk-footer__ BEM"
    - "D-05: bare muted icons (no borders/chips), color var(--tk-text-muted) -> var(--tk-text) on hover, sized to match .tk-footer__link"
    - "D-06: Instagram link -> https://www.instagram.com/tamar_kfir_jewelry/"
    - "D-07: Facebook link -> https://www.facebook.com/tamarkfirjewelry, target=_blank rel=noopener noreferrer + per-icon aria-label"
  artifacts:
    - path: "backend/views/partials/footer.ejs"
      provides: "4th .tk-footer__col with Follow Me heading + two ported social SVG links"
      contains: "tk-footer__social"
    - path: "frontend/css/homepage.css"
      provides: "4-column footer grid + .tk-footer__social bare-muted-icon styles + mobile 2x2 reflow"
      contains: "repeat(4, 1fr)"
  key_links:
    - from: "backend/views/partials/footer.ejs"
      to: "instagram.com/tamar_kfir_jewelry + facebook.com/tamarkfirjewelry"
      via: "external <a href> with target=_blank rel=noopener noreferrer"
      pattern: "href=\"https://www.instagram.com/tamar_kfir_jewelry/\""
    - from: "frontend/css/homepage.css .tk-footer__social-link"
      to: "design tokens"
      via: "color var(--tk-text-muted) -> var(--tk-text) on hover"
      pattern: "tk-footer__social-link:hover"
---

<objective>
Add a 4th "Follow Me" social column (Instagram + Facebook) to the global SSR footer
partial and style it in the existing `.tk-footer` token language, so working social
links render correctly on every page in both LTR (`/en`) and RTL (`/he`) layouts.

Purpose: Satisfy FOOT-01/02/03 — the prototype footer currently has no social section
(the prototype markup omits it by design); the only social markup lives in dead
`View.js` JS that never fires.
Output: Updated `footer.ejs` (4th column with ported SVGs) + `homepage.css`
(3→4 grid, `.tk-footer__social*` rules, mobile reflow).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-footer-social-restore/41-CONTEXT.md
@.planning/phases/41-footer-social-restore/41-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing footer.ejs column structure the new column must mirror (footer.ejs L7-25). -->
<!-- Bilingual idiom: var _eng = lang === 'eng'; then <% if (_eng) { %>EN<% } else { %>HE<% } %> -->
<!-- The new column is the 4th child of .tk-footer__cols, AFTER the Info column, BEFORE </div> at L25. -->
<!-- Existing footer CSS tokens (homepage.css L341-348): --tk-text-muted, --tk-text, --tk-dur, --tk-ease, --tk-size-label, --tk-weight-semibold, --tk-track-label. -->
<!-- Mobile breakpoint is @media (max-width: 860px) at homepage.css L411 (NOT 800px). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 4th "Follow Me" social column to footer.ejs</name>
  <files>backend/views/partials/footer.ejs</files>
  <read_first>
    - backend/views/partials/footer.ejs (the whole file — mirror the existing 3 .tk-footer__col blocks at L7-25 and the `var _eng = lang === 'eng'` idiom at L4)
    - .planning/phases/41-footer-social-restore/41-PATTERNS.md (§footer.ejs — the exact SVG markup to port, L41-67)
    - .planning/phases/41-footer-social-restore/41-CONTEXT.md (D-01, D-03, D-04, D-06, D-07)
    - CLAUDE.md §"SSR + Client Dual-Render" (footer becomes SSR-only after Plan 02 retires the JS twin — no JS mirror needed here)
  </read_first>
  <action>
    Insert a new `<div class="tk-footer__col">` as the 4th and LAST child of
    `.tk-footer__cols` — immediately after the `Info` column's closing `</div>`
    (currently footer.ejs L24) and BEFORE the `</div>` that closes `.tk-footer__cols`
    (L25). Do NOT place it inside `.tk-footer__bar` (D-01).

    Heading uses the existing `.tk-footer__heading` class (NOT the legacy
    `.footer-social-title`) and the established bilingual idiom (D-03):
    `<span class="tk-footer__heading"><% if (_eng) { %>Follow Me<% } else { %>עקבו אחרי<% } %></span>`

    Below the heading add `<div class="tk-footer__social">` containing the TWO social
    `<a>` links. Port the inline SVGs verbatim from View.js setFooterLng() (Instagram =
    outline/stroke, Facebook = solid fill) but rename the legacy class
    `footer-social-link` → `tk-footer__social-link` (BEM namespace, D-04). The SVGs,
    URLs, aria-labels, and target/rel are language-neutral — emit them ONCE, outside any
    `if (_eng)` branch. Do NOT use `urlLang` (these are absolute external URLs). Do NOT
    copy the legacy `style="direction: rtl;"` inline attribute (the CSS grid auto-reverses
    in RTL — PATTERNS correction).

    Exact markup to add:
    ```ejs
    <div class="tk-footer__col">
      <span class="tk-footer__heading"><% if (_eng) { %>Follow Me<% } else { %>עקבו אחרי<% } %></span>
      <div class="tk-footer__social">
        <a class="tk-footer__social-link" href="https://www.instagram.com/tamar_kfir_jewelry/" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
            <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
            <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
          </svg>
        </a>
        <a class="tk-footer__social-link" href="https://www.facebook.com/tamarkfirjewelry" target="_blank" rel="noopener noreferrer" aria-label="Facebook">
          <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M22 12c0-5.523-4.477-10-10-10S2 6.477 2 12c0 4.991 3.657 9.128 8.438 9.878v-6.987H7.898v-2.89h2.54V9.797c0-2.507 1.492-3.89 3.777-3.89 1.094 0 2.238.195 2.238.195v2.46h-1.26c-1.243 0-1.63.771-1.63 1.562V12h2.773l-.443 2.89h-2.33v6.988C18.343 21.128 22 16.991 22 12z"></path>
          </svg>
        </a>
      </div>
    </div>
    ```
  </action>
  <acceptance_criteria>
    - `grep -c 'tk-footer__social' backend/views/partials/footer.ejs` >= 3 (container + 2 links)
    - footer.ejs contains `href="https://www.instagram.com/tamar_kfir_jewelry/"`
    - footer.ejs contains `href="https://www.facebook.com/tamarkfirjewelry"`
    - footer.ejs contains `rel="noopener noreferrer"` (appears at least twice)
    - footer.ejs contains `aria-label="Instagram"` AND `aria-label="Facebook"`
    - footer.ejs contains the EN heading `Follow Me` AND the HE heading `עקבו אחרי`
    - footer.ejs uses class `tk-footer__heading` for the social heading (NOT `footer-social-title`)
    - footer.ejs does NOT contain `style=" direction: rtl;"` or `style="direction: rtl;"`
    - `grep -c 'direction: rtl' backend/views/partials/footer.ejs` == 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'tk-footer__social' backend/views/partials/footer.ejs && grep -q 'instagram.com/tamar_kfir_jewelry' backend/views/partials/footer.ejs && grep -q 'עקבו אחרי' backend/views/partials/footer.ejs && [ "$(grep -c 'direction: rtl' backend/views/partials/footer.ejs)" = "0" ] && echo PASS</automated>
  </verify>
  <done>footer.ejs has a 4th Follow Me column with both ported social SVG links, correct URLs/aria-labels/rel, bilingual heading, and no inline rtl style.</done>
</task>

<task type="auto">
  <name>Task 2: Expand footer grid to 4 columns + add .tk-footer__social styles + mobile reflow</name>
  <files>frontend/css/homepage.css</files>
  <read_first>
    - frontend/css/homepage.css L341-348 (the `.tk-footer*` rule group) and L410-419 (the `@media (max-width: 860px)` block)
    - .planning/phases/41-footer-social-restore/41-PATTERNS.md (§homepage.css — Analog 1/2/3, L71-108)
    - .planning/phases/41-footer-social-restore/41-CONTEXT.md (D-02, D-05)
    - CLAUDE.md §"Frontend Build" (Parcel rebuild + backend restart required after CSS changes)
  </read_first>
  <action>
    1. GRID (homepage.css L343): change `grid-template-columns: repeat(3, 1fr)` →
       `grid-template-columns: repeat(4, 1fr)` (D-01). Four columns at the current
       `max-width: 760px` with `gap: 4rem` is too tight (D-02), so in the SAME rule also
       change `gap: 4rem` → `gap: 3rem` and `max-width: 760px` → `max-width: 920px`.

    2. SOCIAL STYLES — add immediately after the `.tk-footer__link:hover` rule (after
       L347), using ONLY the existing tokens `--tk-text-muted`, `--tk-text`, `--tk-dur`,
       `--tk-ease` (D-05 "bare muted icons", no borders/chips). The ported SVGs use
       `currentColor` for both stroke and fill, so setting `color` on the link drives both:
       ```css
       .tk-footer__social { display: flex; gap: 1rem; margin-top: 0.2rem; }
       .tk-footer__social-link { display: inline-flex; color: var(--tk-text-muted); transition: color var(--tk-dur) var(--tk-ease); }
       .tk-footer__social-link:hover { color: var(--tk-text); }
       .tk-footer__social-link svg { width: 20px; height: 20px; }
       ```

    3. MOBILE REFLOW — in the `@media (max-width: 860px)` block (L411), the
       `.tk-footer__cols` rule currently only sets `gap: 2rem`, leaving 4 columns crammed
       at narrow widths (D-02 forbids this). Change that rule to a 2x2 grid:
       `.tk-footer__cols { gap: 2rem; grid-template-columns: repeat(2, 1fr); }`

    Do NOT hard-code hex colors — palette is locked to the tokens above (D-04/D-05).
    After editing, rebuild the frontend so Parcel picks up the CSS:
    `cd frontend && npm run build` (the backend must be restarted afterward — handled in
    the verification checkpoint, Task 3).
  </action>
  <acceptance_criteria>
    - homepage.css contains `repeat(4, 1fr)` AND no longer contains `repeat(3, 1fr)` on the `.tk-footer__cols` rule
    - homepage.css `.tk-footer__cols` desktop rule contains `max-width: 920px` and `gap: 3rem`
    - homepage.css contains `.tk-footer__social-link` and `.tk-footer__social-link:hover`
    - `grep -v '^ *\*' frontend/css/homepage.css | grep -c 'var(--tk-text-muted)'` increases by 1 (new social-link rest color); social rules reference `var(--tk-text)` on hover, `var(--tk-dur)`, `var(--tk-ease)`
    - homepage.css `.tk-footer__social-link` rules contain NO hex color literal (no `#` in the new social block)
    - the `@media (max-width: 860px)` `.tk-footer__cols` rule contains `repeat(2, 1fr)`
    - `frontend/dist/` rebuilt: `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'repeat(4, 1fr)' frontend/css/homepage.css && grep -q 'tk-footer__social-link' frontend/css/homepage.css && grep -q 'repeat(2, 1fr)' frontend/css/homepage.css && cd frontend && npm run build</automated>
  </verify>
  <done>Footer grid is 4 columns desktop / 2x2 at <=860px; .tk-footer__social* rules use only tokens for the muted->text hover; frontend rebuilt cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify footer social column on LTR, RTL, and a non-home page</name>
  <what-built>
    A 4th "Follow Me" footer column with bare muted Instagram + Facebook icons that
    darken on hover, in the global SSR footer (so it appears on every page), with a 4→2x2
    mobile reflow. Markup is SSR-only; CSS rebuilt into the Parcel bundle.
  </what-built>
  <how-to-verify>
    First restart the backend so it picks up the new bundle hash (per CLAUDE.md
    §"Frontend Build"): in /backend run `npm run devStart` (or restart the running server).
    Then check ALL of:
    1. Visit http://localhost:PORT/en — scroll to footer. The 4th column shows heading
       "FOLLOW ME" (uppercase) with two icons; Instagram (outline) + Facebook (solid).
       Icons are muted grey and darken to near-black on hover.
    2. Click each icon — Instagram opens https://www.instagram.com/tamar_kfir_jewelry/
       and Facebook opens https://www.facebook.com/tamarkfirjewelry, each in a NEW tab.
    3. Visit http://localhost:PORT/he — footer heading reads "עקבו אחרי"; the 4-column
       row mirrors naturally (RTL) and the icon row reads naturally with no overflow.
    4. Visit http://localhost:PORT/en/about (a NON-home SSR page) — confirm the social
       column renders identically there (chrome-rollout CSS-conflict check).
    5. Narrow the window below 860px on any page — the four columns reflow to a tidy 2x2
       (or stacked) layout, NOT four crammed columns overflowing the viewport.
  </how-to-verify>
  <action>Restart the backend to load the new Parcel bundle hash, then perform the how-to-verify checks below. This is a verification-only checkpoint — the implementation was automated in Tasks 1-2; the human confirms visual/RTL/non-home correctness that grep cannot assert.</action>
  <acceptance_criteria>
    - All 5 checks pass on both /en and /he and on /en/about.
    - No console errors related to the footer.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what rendered incorrectly (which page/viewport).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| footer.ejs → external social domains | Outbound links from our page to instagram.com / facebook.com open in a new browsing context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering/Elevation | `target="_blank"` social links in footer.ejs | mitigate | Reverse-tabnabbing: every social `<a target="_blank">` carries `rel="noopener noreferrer"` (D-07). Verified by acceptance criteria grep. |
| T-41-02 | Injection (XSS) | footer.ejs social markup | accept | No user input or dynamic/DB data flows into the social column — heading is a static bilingual literal, URLs/SVGs are hard-coded constants. No interpolation of untrusted data, so no injection surface. |
| T-41-03 | Information disclosure | external referrer leakage | mitigate | `rel="...noreferrer"` also suppresses the Referer header to the external profile, avoiding leaking the originating page URL. |
</threat_model>

<verification>
- `grep -c 'rel="noopener noreferrer"' backend/views/partials/footer.ejs` >= 2
- footer.ejs contains both correct profile URLs and both aria-labels
- homepage.css contains `repeat(4, 1fr)`, `.tk-footer__social-link:hover`, and the `repeat(2, 1fr)` mobile reflow
- Frontend rebuilds cleanly; footer renders the social column on /en, /he, and /en/about
</verification>

<success_criteria>
- FOOT-01: Instagram + Facebook links visible in the `.tk-footer` on every page
- FOOT-02: Links point to instagram.com/tamar_kfir_jewelry/ and facebook.com/tamarkfirjewelry
- FOOT-03: Social column uses the `.tk-footer` token style and lays out correctly in RTL on /he
</success_criteria>

<output>
After completion, create `.planning/phases/41-footer-social-restore/41-01-SUMMARY.md`
</output>
