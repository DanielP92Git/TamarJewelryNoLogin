---
phase: 40-currency-selector-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/View.js
  - frontend/js/locale.js
  - frontend/js/Views/categoriesView.js
autonomous: false
requirements: [CURR-01, CURR-02, CURR-05]

must_haves:
  truths:
    - "Selecting a different currency in the header dropdown flips every category product price (symbol + value) with no page reload or URL change"
    - "The currency-changed event re-renders the category page's subscribing component"
    - "Selected currency persists across page reload and navigation between category pages"
    - "On load the header currency dropdown shows the resolved currency (usd/ils), never the blank default placeholder"
    - "With no saved currency, first-load default follows existing GeoIP detection (Israel -> ils, else -> usd)"
  artifacts:
    - path: "frontend/js/View.js"
      provides: "currency-changed publisher + syncCurrencySelectors on-load sync"
      contains: "syncCurrencySelectors"
    - path: "frontend/js/Views/categoriesView.js"
      provides: "working currency-changed subscriber (reference) + on-load select sync"
      contains: "currency-changed"
    - path: "frontend/js/locale.js"
      provides: "GeoIP first-load currency seed + currency-changed dispatch"
      contains: "currency-changed"
  key_links:
    - from: "frontend/js/View.js change listener"
      to: "window currency-changed event"
      via: "dispatchEvent(new CustomEvent('currency-changed'))"
      pattern: "dispatchEvent.*currency-changed"
    - from: "frontend/js/Views/categoriesView.js"
      to: "displayProducts re-render"
      via: "currency-changed addEventListener"
      pattern: "addEventListener\\('currency-changed'"
    - from: "frontend/js/View.js applySaved"
      to: "header select.value"
      via: "syncCurrencySelectors(getSavedCurrency()) on load"
      pattern: "syncCurrencySelectors\\(getSavedCurrency"
---

<objective>
Verify the currency-changed event reliably drives category-page price re-renders, that the chosen currency persists across reload/navigation, and that the header dropdown always reflects the resolved currency on load (never the blank "default" placeholder, delivering Phase 39's deferred D-08). The publisher (`View.js`), the reference subscriber (`categoriesView.js`), and the GeoIP first-load seed (`locale.js`) already exist — this plan HARDENS them per D-05 (verify, fix only if broken) and D-03 (GeoIP default).

Purpose: CURR-01 / CURR-02 / CURR-05 — currency switching updates all category prices without a URL change, re-renders subscribers, and the selection sticks; the dropdown reflects the resolved currency on every load.
Output: Confirmed-working currency event path; targeted fixes applied only where verification reveals an actual failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-currency-selector-wiring/40-CONTEXT.md
@.planning/phases/40-currency-selector-wiring/40-PATTERNS.md

<constraints>
- CLAUDE.md "SSR + Client Dual-Render": the header (`header.ejs`) is SSR-static. `View.js` may ONLY read/write `select.value` and bind behavior — NEVER `innerHTML`-rewrite the header. All currency work stays on the non-destructive path.
- Keep the dual-class hook on the select: `.header-currency-selector` (JS hook) AND `.tk-nav__currency-select` (style) must both survive. Selectors target `.header-currency-selector` only.
- `View.js` is Parcel-bundled: any code change requires `npm run build` in `/frontend` + a backend restart before the live verification in Task 2.
- No mandatory automated/regression test (D-05 — user chose not to require one). Do NOT add one.
</constraints>

<interfaces>
<!-- The "correct" reference subscriber + the publisher contract. Use these directly. -->

From frontend/js/View.js (publisher + sync, lines 9-78):
```javascript
const CURRENCY_STORAGE_KEY = 'currency';
function normalizeCurrency(value) { /* returns 'usd' | 'ils' | null */ }
function getSavedCurrency() { /* normalizeCurrency(localStorage) || 'usd' */ }
function syncCurrencySelectors(currency) {
  // sets el.value = c on every select.header-currency-selector[name="currency"]
}
// initCurrencyPersistence: applySaved() runs on DOMContentLoaded/immediately,
// document 'change' delegation -> setSavedCurrency -> syncCurrencySelectors
// -> dispatchEvent(new CustomEvent('currency-changed', { detail: { currency } }))
```

From frontend/js/Views/categoriesView.js (REFERENCE subscriber, lines 1031-1067):
```javascript
setupCurrencyHandler() {
  this.selectedCurrency = localStorage.getItem('currency') || 'usd';
  document.querySelectorAll('select.header-currency-selector[name="currency"]')
    .forEach(sel => {
      if (sel.value === 'default' || sel.value !== this.selectedCurrency) sel.value = this.selectedCurrency;
    });
  if (this._currencyListenerAdded) return;
  this._currencyListenerAdded = true;
  window.addEventListener('currency-changed', async e => {
    const next = e?.detail?.currency;
    if (next !== 'usd' && next !== 'ils') return;
    this.selectedCurrency = next;
    this.displayProducts();
  });
}
```

From frontend/js/locale.js (GeoIP first-load seed, lines 121-190):
```javascript
// bootstrapLocaleSync(): if localStorage.currency missing, seeds it from guessLocaleFromBrowser()
//   and sets window.__localeAuto.currencyWasMissing = true
// hydrateLocaleFromBackend(): if currencyWasMissing, on GeoIP result:
//   localStorage.setItem('currency', mapped.appCurrency);
//   window.dispatchEvent(new CustomEvent('currency-changed', { detail: { currency: mapped.appCurrency } }));
//   NOTE: this dispatch does NOT call syncCurrencySelectors itself.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static wiring audit — publisher, category subscriber, on-load sync, GeoIP seed</name>
  <read_first>
    - frontend/js/View.js (lines 9-78: normalizeCurrency, getSavedCurrency, syncCurrencySelectors, initCurrencyPersistence, module-init call)
    - frontend/js/Views/categoriesView.js (lines 1031-1067: setupCurrencyHandler — the reference subscriber)
    - frontend/js/locale.js (lines 121-190: bootstrapLocaleSync + hydrateLocaleFromBackend GeoIP dispatch)
    - .planning/phases/40-currency-selector-wiring/40-PATTERNS.md ("Reference Implementation" + "Pattern Assignments" + "Dropdown reflects resolved currency on load (D-04)")
  </read_first>
  <action>
    Audit the existing currency event path against the categoriesView reference. This is a verify-then-fix gate — apply a fix ONLY when a required piece is genuinely absent or wrong (per D-05). For each check below, confirm the current code, and if (and only if) a check FAILS, apply the cited fix.

    1. PUBLISHER (View.js): confirm `initCurrencyPersistence` (a) is guarded by `window.__currencyPersistenceInitialized`, (b) delegates the `change` listener on `document` (not on the select directly, so it survives header re-render), (c) calls `normalizeCurrency(target.value)` and returns early on `null` (ignores the `default` option), (d) calls `setSavedCurrency` + `syncCurrencySelectors` + `dispatchEvent('currency-changed', { detail: { currency } })`, and (e) is invoked once at module load (lines 76-78). If any piece is missing, restore it to match the lines 42-73 contract in <interfaces>. Expected: ALL present — no change.

    2. ON-LOAD SYNC / D-04 (View.js): confirm `applySaved = () => syncCurrencySelectors(getSavedCurrency())` runs on `DOMContentLoaded` (or immediately if already loaded), lines 46-52. This is what forces the header `<select>` off the `default` placeholder on every page. Expected: present — no change.

    3. CATEGORY SUBSCRIBER (categoriesView.js): confirm `setupCurrencyHandler` (a) syncs the header select off `'default'` on load (lines 1034-1041), (b) guards with `this._currencyListenerAdded`, (c) validates `next !== 'usd' && next !== 'ils'` then returns, (d) re-renders via `this.displayProducts()`. Expected: present (this is the reference) — no change. Also confirm `setupCurrencyHandler` is actually CALLED during the category view's init (grep for `setupCurrencyHandler(` call site). If it is never called, wire the call into the view's render/init path.

    4. GEOIP SEED + D-04 EDGE (locale.js): confirm `bootstrapLocaleSync` seeds `localStorage.currency` from `guessLocaleFromBrowser()` when missing and sets `__localeAuto.currencyWasMissing`, and that `hydrateLocaleFromBackend` dispatches `currency-changed` with the GeoIP currency when `currencyWasMissing`. KNOWN GAP to verify in Task 2: this dispatch does NOT call `syncCurrencySelectors`, so when GeoIP OVERRIDES the browser-guess currency the header `<select>` may still show the guess value until the next change. Do NOT pre-fix. If Task 2 shows the dropdown resting on the wrong/`default` value after a no-saved-currency load, the fix is: import `syncCurrencySelectors` (or call the existing `View.js` exported sync path) and invoke it immediately after `localStorage.setItem(CURRENCY_KEY, mapped.appCurrency)` in `hydrateLocaleFromBackend` (locale.js ~line 173), passing `mapped.appCurrency`. Cite View.js:28-40 as the sync template. Apply only if Task 2 confirms the failure.

    5. CLAUDE'S DISCRETION (low-risk tidy, optional): `categoriesView.js:30` `this.exchangeRate = 3.7` is vestigial (no live conversion — prices come from per-product dual values). If grep confirms it is referenced ONLY as a dead fallback and removing it is trivially safe, remove it; if it is referenced anywhere that would break, LEAVE it. Do not spend effort forcing this.

    Record, in the plan SUMMARY, which checks passed unchanged and any fix actually applied (with the failing symptom that justified it).
  </action>
  <verify>
    <automated>cd frontend && grep -n "dispatchEvent" js/View.js | grep -c "currency-changed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "syncCurrencySelectors(getSavedCurrency" frontend/js/View.js` returns a match (on-load sync present, D-04 path).
    - `grep -c "addEventListener('currency-changed'" frontend/js/Views/categoriesView.js` returns >= 1 (category subscriber present).
    - `grep -n "next !== 'usd' && next !== 'ils'" frontend/js/Views/categoriesView.js` returns a match (payload validation present).
    - `grep -n "dispatchEvent" frontend/js/locale.js` shows a `currency-changed` dispatch in `hydrateLocaleFromBackend`.
    - `grep -rn "setupCurrencyHandler(" frontend/js/Views/categoriesView.js` shows BOTH the definition and at least one call site.
    - No new `innerHTML` write targeting the header/`.tk-nav` was introduced in View.js (grep `innerHTML` in View.js shows no new header rewrite vs current state).
    - If any fix was applied, the SUMMARY names the specific failing check and the cited reference template.
  </acceptance_criteria>
  <done>The publisher, category subscriber, on-load sync, and GeoIP seed are confirmed present and correct (or repaired to match the cited reference). Any applied fix is documented with its justifying symptom. Frontend rebuilt (`cd frontend && npm run build`) and backend restarted so Task 2 exercises the current bundle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — category re-price, persistence, dropdown sync, GeoIP default</name>
  <read_first>
    - .planning/phases/40-currency-selector-wiring/40-CONTEXT.md (D-03, D-04, D-05)
    - frontend/js/Views/categoriesView.js (lines 1403-1461: dual-price formatting at render — what a correct flip looks like)
  </read_first>
  <what-built>
    Task 1 confirmed (and minimally hardened) the currency-changed publisher, the category subscriber, the on-load dropdown sync, and the GeoIP first-load seed. The frontend bundle has been rebuilt and the backend restarted.
  </what-built>
  <how-to-verify>
    Run the backend (`cd backend && npm run devStart`) and open a category page (e.g. `http://localhost:<port>/eng/category/necklaces` — substitute any real category slug). Then:

    1. RE-PRICE (CURR-01 / CURR-02): With the header dropdown on `$ USD`, note a product price (e.g. "$120"). Select `₪ ILS` in the header dropdown. EXPECTED: every visible product price flips to the `₪` symbol and its `ils_price` value, with NO page reload and NO URL change. Switch back to `$ USD`; prices flip back to `$`/usd values.

    2. PERSIST ACROSS RELOAD (CURR-05 + D-04): Leave it on ILS. Reload the page (F5). EXPECTED: prices stay in `₪`/ILS AND the header dropdown still shows `₪ ILS` (NOT the blank "default" placeholder).

    3. PERSIST ACROSS NAVIGATION (CURR-05): Still on ILS, navigate to a different category page. EXPECTED: that page also renders ILS prices and the dropdown shows `₪ ILS`.

    4. GEOIP FIRST-LOAD DEFAULT (D-03): Open devtools console, run `localStorage.removeItem('currency')`, then reload. EXPECTED: prices and the dropdown reflect the GeoIP/browser-resolved currency (Israel -> `₪ ILS`, elsewhere -> `$ USD`) — and the dropdown is NEVER stuck on the blank "default". (If the dropdown shows the wrong/blank value here, this is the locale.js D-04 edge from Task 1 step 4 — report it so the gated fix is applied.)

    If any step fails, describe exactly which step and what you saw (symbol shown, value, whether reload was needed, dropdown resting value). The executor will then apply the documented fix from Task 1 (cite the categoriesView reference / PATTERNS divergence) and rebuild.
  </how-to-verify>
  <acceptance_criteria>
    - Step 1: after selecting ILS, every `.product-price` shows `₪` + the ils value with no reload; reverting to USD shows `$` + usd value.
    - Step 2: after reload on ILS, prices remain `₪`/ILS and the dropdown's displayed option is `₪ ILS` (not "default").
    - Step 3: a second category page renders ILS prices and shows `₪ ILS` selected.
    - Step 4: after clearing `localStorage.currency` and reloading, the dropdown and prices reflect the GeoIP-resolved currency and the dropdown is not on the blank placeholder.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all four steps pass, or describe the failing step (which step, observed symbol/value, dropdown resting value, whether a manual reload was needed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| header `<select>` value -> currency state | User-selectable input crosses into app state; values constrained to the `default`/`usd`/`ils` options. |
| `localStorage.currency` -> price rendering | Persisted value (could be hand-edited) read back on every load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | `localStorage.currency` read in View.js/categoriesView.js | mitigate | Already mitigated: `normalizeCurrency`/`getSavedCurrency` coerce any value to `'usd'`/`'ils'` (else default `'usd'`); the `change` delegate ignores non-`usd/ils` values. Verify no new read path bypasses `normalizeCurrency`. |
| T-40-02 | Tampering | `currency-changed` event payload | accept | Same-origin `window` CustomEvent dispatched only by first-party `View.js`/`locale.js`; subscribers additionally re-validate `detail.currency ∈ {usd,ils}`. No cross-origin reach into `window` events. Low risk. |
| T-40-03 | Information disclosure / Elevation | this phase overall | accept | No auth, no new network calls, no PII, no server state change — pure client-side display/event wiring over a constrained `<select>`. Honest N/A for high-severity STRIDE categories. |
</threat_model>

<verification>
- Currency switching on a category page re-prices all products with no reload/URL change (CURR-01, CURR-02).
- Selection persists across reload and navigation; dropdown reflects it, never "default" (CURR-05, D-04).
- No-saved-currency first load follows GeoIP (D-03).
- No `innerHTML` rewrite of the SSR-static header was introduced (CLAUDE.md dual-render rule held).
- Any code change was gated on a confirmed verification failure (D-05).
</verification>

<success_criteria>
ROADMAP Phase 40 criteria 1, 2, 4 (category scope), and 5 hold: instant re-price, event-driven re-render, retained-across-navigation currency, and a dropdown that always reflects the resolved currency with a GeoIP first-load default.
</success_criteria>

<output>
After completion, create `.planning/phases/40-currency-selector-wiring/40-01-SUMMARY.md` recording which audit checks passed unchanged, any fix applied (with the symptom that justified it), and the live-verification result.
</output>
