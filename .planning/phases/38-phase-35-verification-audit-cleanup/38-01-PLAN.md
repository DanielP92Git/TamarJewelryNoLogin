---
phase: 38-phase-35-verification-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/backup.js
  - .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [MON-02, MON-03, ADM-01, REST-03]
must_haves:
  truths:
    - "Phase 35 VERIFICATION.md exists and confirms all 4 requirements (MON-02, MON-03, ADM-01, REST-03) are satisfied"
    - "Rate limiter in routes/backup.js uses identical header options (standardHeaders: 'draft-7', legacyHeaders: false) as index.js"
    - "REQUIREMENTS.md checkboxes are checked for MON-02, MON-03, ADM-01, REST-03"
    - "REQUIREMENTS.md traceability table shows Phase 35 (not Phase 35 -> 38) and status Complete for all 4 requirements"
  artifacts:
    - path: ".planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md"
      provides: "Verification report for Phase 35 requirements"
      contains: "status: satisfied"
    - path: "backend/routes/backup.js"
      provides: "Fixed rate limiter with draft-7 headers"
      contains: "standardHeaders: 'draft-7'"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes"
      contains: "[x] **MON-02**"
  key_links:
    - from: "35-VERIFICATION.md"
      to: "backend/routes/backup.js"
      via: "ADM-01 evidence references POST /admin/backup route"
      pattern: "POST /admin/backup"
    - from: "35-VERIFICATION.md"
      to: "backend/models/BackupLog.js"
      via: "MON-03 evidence references BackupLog model"
      pattern: "BackupLog"
---

<objective>
Fix the rate limiter header inconsistency in routes/backup.js, create the missing Phase 35 VERIFICATION.md confirming all 4 requirements are satisfied, and update REQUIREMENTS.md checkboxes to reflect completion.

Purpose: Close all 3 gaps identified in the v1.6 milestone audit for Phase 35 — missing verification, unchecked requirements, and rate limiter header mismatch.
Output: Fixed backup.js, new 35-VERIFICATION.md, updated REQUIREMENTS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.6-MILESTONE-AUDIT.md
@.planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-01-SUMMARY.md
@.planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-02-SUMMARY.md
@.planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-UAT.md
@.planning/phases/34-core-backup-service/34-VERIFICATION.md

<interfaces>
<!-- Key files the executor needs to verify against. -->

From backend/routes/backup.js (lines 25-28) -- CURRENT (broken):
```javascript
const adminRateLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  limit: Number(process.env.RATE_LIMIT_ADMIN_MAX || 120),
});
```

From backend/index.js (lines 281-285) -- TARGET (reference):
```javascript
const adminRateLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15m
  limit: Number(process.env.RATE_LIMIT_ADMIN_MAX || 120),
  standardHeaders: 'draft-7',
  legacyHeaders: false,
  message: {
    success: false,
    error: 'Too many requests, please try again later.',
  },
});
```

Phase 35 production files to verify:
- backend/models/BackupLog.js -- Mongoose model for backup run history
- backend/services/backupAlertService.js -- sendBackupFailureAlert() via EmailJS
- backend/routes/backup.js -- POST /admin/backup + GET /admin/backups
- backend/jobs/backupJob.js -- cron callback with BackupLog + alert
- backend/models/index.js -- BackupLog barrel export
- backend/index.js -- app.use('/admin', backupRoutes) wiring
- backend/env.example -- 5 EmailJS env vars

Phase 35 test files (95 tests):
- backend/tests/unit/models/backupLog.test.js (31 tests)
- backend/tests/unit/services/backupAlertService.test.js (15 tests)
- backend/tests/integration/backup.trigger.test.js (19 tests)
- backend/tests/integration/backup.listing.test.js (17 tests)
- backend/tests/unit/jobs/backupJob.test.js (13 tests, 7 new for Phase 35)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rate limiter headers and create Phase 35 VERIFICATION.md</name>
  <files>backend/routes/backup.js, .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md</files>
  <read_first>
    - backend/routes/backup.js (the file being modified -- see current rate limiter at lines 25-28)
    - backend/index.js (lines 280-290 -- reference implementation of adminRateLimiter with standardHeaders/legacyHeaders)
    - backend/models/BackupLog.js (MON-03 evidence -- schema fields)
    - backend/services/backupAlertService.js (MON-02 evidence -- email alert function)
    - backend/jobs/backupJob.js (MON-02/MON-03 evidence -- cron persistence + alerting)
    - backend/models/index.js (BackupLog barrel export)
    - backend/env.example (EmailJS env vars for MON-02)
    - .planning/phases/34-core-backup-service/34-VERIFICATION.md (format reference for VERIFICATION.md)
    - .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-01-SUMMARY.md (what was built)
    - .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-02-SUMMARY.md (what was tested)
    - .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-UAT.md (UAT results -- 8/8 passed)
  </read_first>
  <action>
**Part A: Fix rate limiter in routes/backup.js (lines 25-28)**

Replace the adminRateLimiter block at lines 25-28 with:

```javascript
const adminRateLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  limit: Number(process.env.RATE_LIMIT_ADMIN_MAX || 120),
  standardHeaders: 'draft-7',
  legacyHeaders: false,
});
```

This adds `standardHeaders: 'draft-7'` and `legacyHeaders: false` to match the index.js adminRateLimiter. Do NOT add the `message` property -- the backup routes return their own JSON error responses in each handler, so the rate limiter's default 429 message is acceptable (it only fires on abuse, not normal operation).

**Part B: Create 35-VERIFICATION.md**

Create `.planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md` following the format of 34-VERIFICATION.md. The verification must include:

Frontmatter fields (use standard YAML frontmatter delimiters):
- phase: 35-manual-trigger-backup-listing-failure-alerting
- verified: (current ISO timestamp)
- status: satisfied
- score: 4/4 requirements verified
- overrides_applied: 0

Phase Goal: "Admin can trigger a backup on demand, view backup run history, and receive an email notification when a backup fails"

**Observable Truths** (from Phase 35 roadmap success criteria):

1. "Authenticated admin can POST to /backup and receive a synchronous response showing the result" -- Verify by grep: `router.post('/backup'` in routes/backup.js, `res.json({ success: true, status: result.status` in routes/backup.js, `fetchUser, requireAdmin` middleware applied.

2. "Authenticated admin can GET /backups and receive a list of all available Spaces backup objects" -- Verify by grep: `router.get('/backups'` in routes/backup.js, `res.json({ success: true, backups: merged })` in routes/backup.js.

3. "Backup run history is persisted in a backup_logs MongoDB collection" -- Verify: `backend/models/BackupLog.js` exists with `mongoose.model('BackupLog', ...)`, collection name `backup_logs`, fields: timestamp, status, filename, bytes, duration_ms, error, trigger. Both route and cron persist via `BackupLog.create()`.

4. "When a backup fails, an email alert is sent via EmailJS" -- Verify: `backend/services/backupAlertService.js` exports `sendBackupFailureAlert`, uses `fetch('https://api.emailjs.com/api/v1.6/email/send')`, guards for missing env vars (EMAILJS_SERVICE_ID, EMAILJS_ALERT_TEMPLATE_ID, EMAILJS_PUBLIC_KEY). Both `routes/backup.js` and `jobs/backupJob.js` call `sendBackupFailureAlert(result)` when `result.status === 'failed'`.

**Required Artifacts** -- List all 7 production files + 5 test files with line counts and export verification.

**Key Link Verification** -- Verify these 7 wiring paths:
- routes/backup.js -> backupService.js (runBackup, createBackupS3Client imports)
- routes/backup.js -> BackupLog (model import for create)
- routes/backup.js -> backupAlertService (sendBackupFailureAlert import)
- routes/backup.js -> middleware/auth (fetchUser, requireAdmin imports)
- jobs/backupJob.js -> BackupLog (model import for cron persistence)
- jobs/backupJob.js -> backupAlertService (alert on cron failure)
- index.js -> routes/backup.js (app.use('/admin', backupRoutes))

**Behavioral Spot-Checks** -- Run:
- `node -e "const r = require('./backend/routes/backup'); console.log(typeof r)"` to confirm module loads
- `node -e "const m = require('./backend/models/BackupLog'); console.log(m.modelName)"` to confirm BackupLog model
- `node -e "const s = require('./backend/services/backupAlertService'); console.log(typeof s.sendBackupFailureAlert)"` to confirm alert export

**Requirements Coverage** table:
- ADM-01: POST /admin/backup with fetchUser + requireAdmin + runBackup() -> SATISFIED
- REST-03: GET /admin/backups with S3 listObjectsV2 + BackupLog merge -> SATISFIED
- MON-02: sendBackupFailureAlert called on result.status==='failed' in both route and cron -> SATISFIED
- MON-03: BackupLog.create() called in both POST /admin/backup handler and backupJob cron callback -> SATISFIED

**Test Evidence** -- Reference the 95 tests from 35-02-SUMMARY.md and 8/8 UAT from 35-UAT.md.
  </action>
  <verify>
    <automated>cd C:/Development/Online && grep -n "standardHeaders" backend/routes/backup.js && grep -n "legacyHeaders" backend/routes/backup.js && test -f .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md && grep "status: satisfied" .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/routes/backup.js contains `standardHeaders: 'draft-7',` on a line between `limit:` and the closing `});` of adminRateLimiter
    - backend/routes/backup.js contains `legacyHeaders: false,` on a line between `standardHeaders` and the closing `});` of adminRateLimiter
    - .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md exists
    - 35-VERIFICATION.md frontmatter contains `status: satisfied`
    - 35-VERIFICATION.md frontmatter contains `score: 4/4`
    - 35-VERIFICATION.md body contains `ADM-01` with `SATISFIED`
    - 35-VERIFICATION.md body contains `REST-03` with `SATISFIED`
    - 35-VERIFICATION.md body contains `MON-02` with `SATISFIED`
    - 35-VERIFICATION.md body contains `MON-03` with `SATISFIED`
    - 35-VERIFICATION.md body contains `## Observable Truths` section
    - 35-VERIFICATION.md body contains `## Required Artifacts` section
    - 35-VERIFICATION.md body contains `## Key Link Verification` section
    - 35-VERIFICATION.md body contains `## Requirements Coverage` section
    - All Phase 35 tests still pass: `cd backend && npx vitest run tests/unit/models/backupLog.test.js tests/unit/services/backupAlertService.test.js tests/integration/backup.trigger.test.js tests/integration/backup.listing.test.js tests/unit/jobs/backupJob.test.js` exits 0
  </acceptance_criteria>
  <done>Rate limiter in routes/backup.js has standardHeaders:'draft-7' and legacyHeaders:false matching index.js. 35-VERIFICATION.md exists with status:satisfied and all 4 requirements (MON-02, MON-03, ADM-01, REST-03) marked SATISFIED with evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified -- current state of checkboxes and traceability table)
    - .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md (just created in Task 1 -- confirms requirements are satisfied)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` to make these exact changes:

**Checkbox updates (lines 20-22, 25, 33):**

Change line 21 from `- [ ] **MON-02**: Failed backups trigger an email alert via EmailJS` to `- [x] **MON-02**: Failed backups trigger an email alert via EmailJS`

Change line 22 from `- [ ] **MON-03**: Backup run history is persisted in a MongoDB `backup_logs` collection` to `- [x] **MON-03**: Backup run history is persisted in a MongoDB `backup_logs` collection`

Change line 25 from `- [ ] **ADM-01**: Admin can trigger a manual backup via authenticated POST endpoint` to `- [x] **ADM-01**: Admin can trigger a manual backup via authenticated POST endpoint`

Change line 33 from `- [ ] **REST-03**: Admin can view list of available backups (GET endpoint)` to `- [x] **REST-03**: Admin can view list of available backups (GET endpoint)`

**Traceability table updates (lines 69-76):**

Change line 69 from `| MON-02 | Phase 35 → 38 | Pending |` to `| MON-02 | Phase 35 | Complete |`

Change line 70 from `| MON-03 | Phase 35 → 38 | Pending |` to `| MON-03 | Phase 35 | Complete |`

Change line 71 from `| ADM-01 | Phase 35 → 38 | Pending |` to `| ADM-01 | Phase 35 | Complete |`

Change line 76 from `| REST-03 | Phase 35 → 38 | Pending |` to `| REST-03 | Phase 35 | Complete |`

Update the "Last updated" line at the bottom to: `*Last updated: {today's date} after Phase 38 gap closure (MON-02, MON-03, ADM-01, REST-03 verified)*`
  </action>
  <verify>
    <automated>cd C:/Development/Online && grep "\[x\] \*\*MON-02\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*MON-03\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*ADM-01\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*REST-03\*\*" .planning/REQUIREMENTS.md && grep "| MON-02 | Phase 35 | Complete |" .planning/REQUIREMENTS.md && grep "| MON-03 | Phase 35 | Complete |" .planning/REQUIREMENTS.md && grep "| ADM-01 | Phase 35 | Complete |" .planning/REQUIREMENTS.md && grep "| REST-03 | Phase 35 | Complete |" .planning/REQUIREMENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `[x] **MON-02**`
    - .planning/REQUIREMENTS.md contains `[x] **MON-03**`
    - .planning/REQUIREMENTS.md contains `[x] **ADM-01**`
    - .planning/REQUIREMENTS.md contains `[x] **REST-03**`
    - .planning/REQUIREMENTS.md contains `| MON-02 | Phase 35 | Complete |`
    - .planning/REQUIREMENTS.md contains `| MON-03 | Phase 35 | Complete |`
    - .planning/REQUIREMENTS.md contains `| ADM-01 | Phase 35 | Complete |`
    - .planning/REQUIREMENTS.md contains `| REST-03 | Phase 35 | Complete |`
    - .planning/REQUIREMENTS.md does NOT contain `Phase 35 → 38` for any of the 4 requirements
    - .planning/REQUIREMENTS.md last updated line references Phase 38 gap closure
  </acceptance_criteria>
  <done>All 4 Phase 35 requirement checkboxes (MON-02, MON-03, ADM-01, REST-03) are checked [x] and traceability table shows Phase 35 / Complete for each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> admin API | Rate-limited admin endpoints (already protected by fetchUser + requireAdmin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Spoofing | POST /admin/backup | accept | Already mitigated by fetchUser + requireAdmin auth middleware (Phase 35). No change in this phase. |
| T-38-02 | Information Disclosure | Rate limiter headers | mitigate | Adding standardHeaders: 'draft-7' exposes RateLimit-* headers to clients. These headers are informational (remaining requests, reset time) and already exposed on index.js admin endpoints. Consistent behavior across all admin routes. |
| T-38-03 | Denial of Service | Rate limiter | accept | Rate limit enforcement (120 req/15 min) is unchanged. Header options only affect response metadata, not limit enforcement. |

No high-severity threats identified. This phase makes a minor consistency fix and creates documentation artifacts.
</threat_model>

<verification>
1. Rate limiter fix verified: `grep -A2 "standardHeaders" backend/routes/backup.js` shows `'draft-7'` and `legacyHeaders: false`
2. VERIFICATION.md exists: `test -f .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md`
3. VERIFICATION.md status: `grep "status: satisfied" .planning/phases/35-manual-trigger-backup-listing-failure-alerting/35-VERIFICATION.md`
4. All 4 requirements checked: `grep -c "\[x\]" .planning/REQUIREMENTS.md` shows 10 (was 6, now 10)
5. Phase 35 tests still pass: `cd backend && npx vitest run tests/unit/models/backupLog.test.js tests/unit/services/backupAlertService.test.js tests/integration/backup.trigger.test.js tests/integration/backup.listing.test.js tests/unit/jobs/backupJob.test.js`
</verification>

<success_criteria>
- 35-VERIFICATION.md exists with status:satisfied and all 4 requirements marked SATISFIED with grep-verifiable evidence
- backend/routes/backup.js adminRateLimiter contains standardHeaders:'draft-7' and legacyHeaders:false
- REQUIREMENTS.md has [x] for MON-02, MON-03, ADM-01, REST-03
- REQUIREMENTS.md traceability shows Phase 35 / Complete (not Phase 35 -> 38 / Pending)
- All Phase 35 tests pass (no regressions from rate limiter fix)
</success_criteria>

<output>
After completion, create `.planning/phases/38-phase-35-verification-audit-cleanup/38-01-SUMMARY.md`
</output>
