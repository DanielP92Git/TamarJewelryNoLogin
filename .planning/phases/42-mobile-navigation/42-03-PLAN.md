---
phase: 42-mobile-navigation
plan: 03
type: execute
wave: 3
depends_on: [42-01, 42-02]
files_modified:
  - frontend/dist
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]
must_haves:
  truths:
    - "The freshly built bundle (new content hash) is served by the backend so the new View.js + CSS reach the page"
    - "Below 800px the hamburger is visible and desktop nav links are hidden; at >=800px the reverse"
    - "Tapping the hamburger opens the overlay with all 5 links; it dismisses via re-tap, close button, and outside/scrim tap"
    - "Language flags + currency are reachable inside the overlay on mobile and changing currency updates prices"
    - "On /he the overlay mirrors correctly (close button top-left) and no '[View] Menu bars button not found' appears in the console; open state survives the in-page language toggle"
  artifacts:
    - path: "frontend/dist/index.html"
      provides: "Rebuilt bundle with new content-hashed script/css tags"
      contains: "<script"
  key_links:
    - from: "frontend/dist bundle"
      to: "served SSR pages (bundleScripts)"
      via: "backend restart re-reads dist/index.html at startup"
      pattern: "dist/index.html"
---

<objective>
Rebuild the frontend bundle, restart the backend so it picks up the new content-hashed bundle, and verify the mobile navigation end-to-end across breakpoint, dismissal methods, RTL, and language-toggle persistence.

Purpose: The backend reads `dist/index.html` at startup to extract `<script>` tags (`bundleScripts`). Without a clean rebuild + restart, the Wave-2 View.js and Wave-1 CSS changes never reach the served pages.
Output: New bundle built and served; automated DOM/source checks pass; a human verifies the responsive + interaction + RTL behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-mobile-navigation/42-UI-SPEC.md
@.planning/phases/42-mobile-navigation/42-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean rebuild frontend bundle and restart backend</name>
  <files>frontend/dist</files>
  <read_first>
    - CLAUDE.md ("Frontend Build" section — clean build with `rm -rf dist && npm run build`; backend must be restarted to pick up new bundle hashes)
    - frontend/package.json (confirm the `build` script)
    - backend/package.json (confirm the dev/start script)
  </read_first>
  <action>
Run a clean frontend build so stale hashed files do not linger:

```bash
cd C:/Development/Online/frontend && rm -rf dist && npm run build
```

Confirm `frontend/dist/index.html` was regenerated with fresh content-hashed `<script>`/CSS references. Then restart the backend so it re-reads `dist/index.html` at startup and refreshes the `bundleScripts` variable used by the EJS templates.

If a backend dev server is already running, stop it and start it again with `cd C:/Development/Online/backend && npm run devStart` (nodemon). If running detached for verification, ensure it is reachable on its usual local port. Do NOT add `crossorigin` to any emitted tags.

This task only regenerates build artifacts — it modifies no source files.
  </action>
  <verify>
    <automated>cd "C:/Development/Online" && test -f frontend/dist/index.html && grep -c '<script' frontend/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/dist/index.html` exists and was modified during this task (fresh build)
    - `grep -c '<script' frontend/dist/index.html` returns >= 1 (bundle script tags present)
    - The build command exited 0 (no Parcel build errors)
    - Backend restarted after the build (so `bundleScripts` reflects the new hashes)
  </acceptance_criteria>
  <done>A clean `dist/` is built and the backend has been restarted against it.</done>
</task>

<task type="auto">
  <name>Task 2: Automated source/markup verification of the served chrome</name>
  <files>frontend/dist</files>
  <read_first>
    - backend/views/partials/header.ejs (Wave-1 output)
    - frontend/css/homepage.css (Wave-1 output)
    - frontend/js/View.js (Wave-2 output)
  </read_first>
  <action>
Run a consolidated check that the three source files carry the complete, consistent contract before the human verification step. This guards against a partial Wave-1/Wave-2 application. Confirm: hamburger + overlay markup present, unique currency IDs, 800px breakpoint single-source, overlay CSS present, and the toggle wired with svgHandler fully retired.

Use the verify command below; all counts must match the acceptance criteria exactly.
  </action>
  <verify>
    <automated>cd "C:/Development/Online" && echo "ham:$(grep -c 'class=\"tk-hamburger\"' backend/views/partials/header.ejs)" && echo "overlay:$(grep -c 'id=\"tk-mobile-nav\"' backend/views/partials/header.ejs)" && echo "curmob:$(grep -c 'id=\"currency-mobile\"' backend/views/partials/header.ejs)" && echo "curdesk:$(grep -c 'id=\"currency-desktop\"' backend/views/partials/header.ejs)" && echo "css860:$(grep -c 'max-width: 860px' frontend/css/homepage.css)" && echo "cssoverlay:$(grep -c '#tk-mobile-nav' frontend/css/homepage.css)" && echo "bind:$(grep -c 'this._bindHamburgerMenu()' frontend/js/View.js)" && echo "svg:$(grep -c 'svgHandler' frontend/js/View.js)"</automated>
  </verify>
  <acceptance_criteria>
    - `ham` = 1, `overlay` = 1 (header markup present)
    - `curmob` = 1 AND `curdesk` = 1 (both currency selects present, unique IDs — no duplicate-ID collision)
    - `css860` = 0 (breakpoint retargeted to 800px) AND `cssoverlay` >= 1 (overlay styled)
    - `bind` = 1 (toggle call wired in hydratePrototypeChrome)
    - `svg` = 0 (legacy svgHandler fully retired)
  </acceptance_criteria>
  <done>All three source files carry the consistent Phase-42 contract; no partial application.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — responsive, dismissal, currency, RTL, persistence</name>
  <what-built>
A hamburger button + full-screen white overlay mobile menu in the global chrome: appears below 800px, exposes all 5 nav links, dismisses three ways (+ Escape), relocates flags + currency into the overlay, mirrors for RTL on /he, and survives the in-page language toggle — all without destructive chrome rewrites. The legacy `svgHandler()` is retired.
  </what-built>
  <how-to-verify>
With the backend running and the fresh bundle served, open the site locally and check, in order:

1. **Breakpoint (NAV-01):** At browser width >= 800px the desktop nav links show and NO hamburger appears. Narrow below 800px: the desktop links disappear and the hamburger appears at the far end of the bar (logo + cart + hamburger only). Open DevTools console — confirm there is NO `'[View] Menu bars button not found'` message.
2. **Open (NAV-02):** Below 800px, tap the hamburger. The white full-screen overlay fades/scales in showing all 5 links (Home, Shop, Jewelry Workshop, About, Contact Me) centered, with the current page's link in gold. Page body scroll is locked while open.
3. **Three dismissals (NAV-03):** (a) tap the hamburger again → closes; (b) reopen, tap the close ✕ (top-right) → closes; (c) reopen, tap empty white space outside the links → closes; (d) reopen, press Escape → closes. After each close, body scroll is restored.
4. **Language + currency on mobile (NAV-04):** Reopen the overlay. At the bottom strip, the two flags and the currency dropdown are present. Change the currency dropdown → prices on the page update (no URL language switch). Tap a flag → navigates to the other-language SSR URL.
5. **RTL (NAV-05):** Visit a `/he` page below 800px. The hamburger sits at the far LEFT of the bar; open the overlay — the close ✕ is at the TOP-LEFT (mirrored). Links render in Hebrew, centered.
6. **Persistence (NAV-05):** On `/he` or via the in-page language behavior, confirm interacting with language does NOT throw a console error and the overlay/chrome is not visibly rebuilt/flashed. Reopen and re-dismiss to confirm the toggle still works after a language interaction (idempotent bind, no double-fire).
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe which check failed (with screen width + console output).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Build + verification only. No new runtime data flows; the served pages are the same SSR chrome with the Phase-42 markup/CSS/JS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Tampering | served bundle freshness | mitigate | Clean `rm -rf dist && npm run build` + backend restart ensures the verified source is exactly what is served (no stale hash serving old behavior). |
| T-42-08 | Information disclosure | DevTools console during verify | accept | Verification step only reads console for the absence of the legacy error; no secrets logged. |
</threat_model>

<verification>
- Fresh `dist/index.html` built and served; backend restarted.
- Automated counts confirm consistent Wave-1 + Wave-2 application (hamburger, overlay, unique currency IDs, 800px single-source, svgHandler retired).
- Human confirms the six interaction/RTL/persistence checks pass.
</verification>

<success_criteria>
- NAV-01: hamburger appears <800px with desktop links hidden; no legacy console error.
- NAV-02: tap opens overlay with all 5 links.
- NAV-03: re-tap, close button, outside tap (and Escape) all dismiss.
- NAV-04: flags + currency reachable in overlay; currency change updates prices.
- NAV-05: RTL mirrors correctly on /he; open state survives language interaction with no destructive rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/42-mobile-navigation/42-03-SUMMARY.md`
</output>
