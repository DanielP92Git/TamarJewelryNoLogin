---
phase: 39-header-utilities-layout
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HEADER-01, HEADER-03, HEADER-04]

must_haves:
  truths:
    - "ROADMAP Phase 39 success criteria 1/3/4 match the locked decisions (two flag icons, Flags->Currency->Cart, RTL cart-left/flags-right)"
    - "REQUIREMENTS HEADER-01 no longer describes a single rounded pill"
    - "No stale 'single rounded pill' / 'flag pill' / cart-leftmost wording remains for Phase 39"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 39 success criteria 1/3/4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended HEADER-01 wording"
  key_links:
    - from: "ROADMAP/REQUIREMENTS Phase 39 criteria"
      to: "39-CONTEXT.md locked decisions D-01/D-04/D-09"
      via: "documentation sync so verification aligns with implementation"
      pattern: "two .* flag"
---

<objective>
Apply the doc-sync amendments flagged in 39-CONTEXT.md §Deferred so the ROADMAP/REQUIREMENTS acceptance wording matches the locked decisions (D-01, D-03, D-04, D-09). This prevents phase verification from failing against stale criteria that still describe a "single rounded pill" and a cart-leftmost order.

Purpose: Align HEADER-01 (REQUIREMENTS) and Phase 39 success criteria 1/3/4 (ROADMAP) with the implemented design. Pure documentation — no code.
Output: Amended `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.

> FLAG: This plan is documentation-only and was explicitly deferred/approved in 39-CONTEXT.md (§Deferred + the three "⚠ ROADMAP edit required" notes). It does not touch the product. If the orchestrator prefers to keep doc-sync out of the implementation phase, this plan can be skipped without affecting the header behavior — but verification will then need to read the locked decisions, not the stale criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-header-utilities-layout/39-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

The locked decisions that the stale wording must be amended to match:
- D-01: TWO separate round SVG flag icons (NOT a single combined pill). The "one rounded pill" idea is dropped.
- D-03: active flag = full color + full opacity; inactive dimmed ~0.4 (no gold ring).
- D-04: LTR order Flags -> Currency -> Cart (cart at the far right).
- D-09: RTL true mirror -> visually Cart (left) -> Currency -> Flags (right).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP criteria 1/3/4 and REQUIREMENTS HEADER-01</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 39 section, lines ~119-129; the list-item line ~112)
    - .planning/REQUIREMENTS.md (HEADER-01 line ~12; HEADER-03 line ~14)
    - .planning/phases/39-header-utilities-layout/39-CONTEXT.md (the three "⚠ ROADMAP edit required" notes under D-01, D-04, D-09)
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, Phase 39 "Success Criteria" block (lines ~123-127):
    - Criterion 1: replace "Visitor sees a single rounded pill containing both IL and US flags side by side..." with:
      "Visitor sees two separate round flag icons (US + IL) in the header utilities area on both `/en` and `/he` — the active language's flag is full color, the inactive one is dimmed (~0.4 opacity, no ring)."
    - Criterion 3: replace "...displays cart icon + item count, currency dropdown, and flag pill in the approved left-to-right sequence with correct spacing" with:
      "...displays the utilities in the approved left-to-right sequence Flags -> Currency -> Cart (cart icon + item count at the far right edge) with correct spacing."
    - Criterion 4: replace "...mirrors for RTL — flag pill at the left, cart at the right..." with:
      "...mirrors for RTL as a true mirror — cart at the left, flags at the right (order Cart -> Currency -> Flags) — without breaking the nav link centering or the TK logo position."
    - Also update the Phase 39 list-item line (~112) and the "Goal" line (~120): replace any "flag pill" phrasing with "two refined flag icons".

    In `.planning/REQUIREMENTS.md`:
    - HEADER-01 (line ~12): replace "Visitor sees EN/HE flags as a single rounded pill (IL + US)..." with:
      "Visitor sees EN/HE as two separate refined flag icons (US + IL) in the header utilities cluster — active flag full color, inactive dimmed (no ring)."
    - HEADER-03 (line ~14): replace "...and flag pill in the approved order..." with "...and flags in the approved order Flags -> Currency -> Cart...".

    Do NOT change requirement IDs, the traceability table, or any other phase. Keep markdown checkbox/bullet structure intact.
  </action>
  <acceptance_criteria>
    - ROADMAP.md Phase 39 section contains "two separate round flag icons" (or "two refined flag icons")
    - ROADMAP.md does NOT contain "single rounded pill" anywhere in the Phase 39 section
    - ROADMAP.md Phase 39 criterion 3 contains the sequence "Flags -> Currency -> Cart" (or "Flags → Currency → Cart")
    - ROADMAP.md Phase 39 criterion 4 contains "cart at the left" and "flags at the right"
    - ROADMAP.md Phase 39 section contains no remaining "flag pill" phrasing
    - REQUIREMENTS.md HEADER-01 contains "two separate" and does NOT contain "single rounded pill"
    - REQUIREMENTS.md HEADER-03 contains no "flag pill" phrasing
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Development/Online && rg -n "single rounded pill|flag pill" .planning/ROADMAP.md .planning/REQUIREMENTS.md; echo "expect: no matches"</automated>
    <automated>cd C:/Development/Online && rg -n "two separate|two refined" .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>ROADMAP criteria 1/3/4 and REQUIREMENTS HEADER-01/03 describe two flag icons and the Flags->Currency->Cart order (cart-left/flags-right in RTL); no "pill" wording remains for Phase 39.</done>
</task>

</tasks>

<threat_model>
Documentation-only change to planning markdown. No code, no trust boundary, no runtime surface. No STRIDE threats applicable.
</threat_model>

<verification>
- No "single rounded pill" / "flag pill" wording remains for Phase 39 in ROADMAP or REQUIREMENTS.
- Amended criteria match D-01/D-04/D-09 (two icons, Flags->Currency->Cart, RTL cart-left/flags-right).
</verification>

<success_criteria>
Acceptance_criteria pass via grep; only ROADMAP.md and REQUIREMENTS.md modified; IDs and traceability table untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/39-header-utilities-layout/39-03-SUMMARY.md`
</output>
