---
phase: 39-header-utilities-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/views/partials/header.ejs
  - frontend/js/View.js
autonomous: true
requirements: [HEADER-01, HEADER-02]

must_haves:
  truths:
    - "Currency options read `$ USD` and `₪ ILS` on both /en and /he"
    - "Currency labels stay `$ USD` / `₪ ILS` AFTER View.js hydration (not reverted by updateCurrencySelectorText)"
    - "Each flag carries aria-label + title; the active flag carries aria-current=\"true\""
    - "hydratePrototypeChrome performs no innerHTML rewrite of the header (CSS/structure preserved)"
  artifacts:
    - path: "backend/views/partials/header.ejs"
      provides: "Currency option labels `$ USD` / `₪ ILS`; flag title + aria-current"
      contains: "$ USD"
    - path: "frontend/js/View.js"
      provides: "updateCurrencySelectorText mirrors `$ USD` / `₪ ILS` so SSR labels persist"
      contains: "$ USD"
  key_links:
    - from: "backend/views/partials/header.ejs option text"
      to: "frontend/js/View.js updateCurrencySelectorText"
      via: "dual-render mirror (CLAUDE.md) — JS sets options[].text on every load"
      pattern: "\\$ USD"
---

<objective>
Set the currency option labels to `$ USD` / `₪ ILS` (D-07) in the SSR header AND mirror them in the matching `View.js` hydration code so the labels are not silently reverted, plus add the flag accessibility attributes (title + aria-current) required by the UI-SPEC.

Purpose: HEADER-01 (flag a11y) + HEADER-02 (currency option copy). Completes the markup/JS half of Phase 39.
Output: Modified `backend/views/partials/header.ejs` and `frontend/js/View.js`. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-header-utilities-layout/39-CONTEXT.md
@.planning/phases/39-header-utilities-layout/39-UI-SPEC.md
@.planning/phases/39-header-utilities-layout/39-PATTERNS.md

CRITICAL dual-render finding (CLAUDE.md "SSR + Client Dual-Render"): `header.ejs` is SSR-static, but `View.js hydratePrototypeChrome` (lines 981-1016) calls `updateCurrencySelectorText` (lines 1047-1071) on every page load, which **reassigns** `options[1].text` / `options[2].text` to `USD`/`ILS` (eng) and `דולר`/`שקל` (heb). If only `header.ejs` is changed, the JS will overwrite the new `$ USD` / `₪ ILS` labels a split-second after load. Therefore the SSR change MUST be mirrored in `updateCurrencySelectorText`. This is a two-string-literal change, NOT a destructive `innerHTML` rewrite — it keeps the SSR-static-chrome rule intact (no header re-render, JS hooks untouched).

Do NOT touch `getCurrencySelectorMarkup` (lines ~1018-1045) — that builds the legacy `.menu`-path select and does not run on prototype chrome (per CONTEXT §code_context).

Keep all JS-hook identifiers: `id="currency-desktop"`, `class="header-currency-selector"`, `class="currency-option"`, `value="default"/"usd"/"ils"`, flag `data-lang`, `flag-icon`, `flag-eng`/`flag-heb`. Phase 40 reads the option `value`s.

<interfaces>
Current header.ejs select markup (backend/views/partials/header.ejs lines 44-48):
```html
<option value="usd" class="currency-option"><% if (_eng) { %>USD<% } else { %>דולר<% } %></option>
<option value="ils" class="currency-option"><% if (_eng) { %>ILS<% } else { %>שקל<% } %></option>
```

Current View.js updateCurrencySelectorText (frontend/js/View.js lines 1053-1062):
```js
if (lng === 'eng') {
  currencySelect.options[0].text = 'Currency';
  currencySelect.options[1].text = 'USD';
  currencySelect.options[2].text = 'ILS';
  currencySelect.removeAttribute('dir');
} else if (lng === 'heb') {
  currencySelect.options[0].text = 'מטבע';
  currencySelect.options[1].text = 'דולר';
  currencySelect.options[2].text = 'שקל';
  currencySelect.setAttribute('dir', 'rtl');
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: header.ejs — currency labels `$ USD` / `₪ ILS` + flag a11y</name>
  <read_first>
    - backend/views/partials/header.ejs (full file — 57 lines; flags 23 & 30, select 44-48)
    - .planning/phases/39-header-utilities-layout/39-UI-SPEC.md (§Copywriting Contract; §Component Inventory 1 accessibility)
    - .planning/phases/39-header-utilities-layout/39-PATTERNS.md (§4)
  </read_first>
  <action>
    In `backend/views/partials/header.ejs`:

    A. Currency options (lines 45-47): replace the bilingual conditionals on the USD and ILS options with a single label for BOTH languages (D-07). Keep the `default` option's bilingual `Currency`/`מטבע` conditional unchanged.
       - USD option becomes: `<option value="usd" class="currency-option">$ USD</option>`
       - ILS option becomes: `<option value="ils" class="currency-option">₪ ILS</option>`
       Do NOT change `value=`, `class="currency-option"`, the select's `id="currency-desktop"`, `name="currency"`, `.header-currency-selector` class, or the `<% if (!_eng) { %> dir="rtl"<% } %>` on the select.

    B. Flag accessibility (lines 23 & 30). The flags already have `role="button" tabindex="0"` and `aria-label="English"` / `aria-label="עברית"`. ADD a matching `title` (hover tooltip) and `aria-current="true"` on the active flag:
       - US flag (`flag-eng`, line 23): add `title="English"` and, gated on the same condition as `selected`, `aria-current`:
         `<div class="flag-icon flag-eng<%= _eng ? ' selected' : '' %>" data-lang="eng" role="button" tabindex="0" aria-label="English" title="English"<% if (_eng) { %> aria-current="true"<% } %>>`
       - IL flag (`flag-heb`, line 30): add `title="עברית"` and aria-current gated on `!_eng`:
         `<div class="flag-icon flag-heb<%= !_eng ? ' selected' : '' %>" data-lang="heb" role="button" tabindex="0" aria-label="עברית" title="עברית"<% if (!_eng) { %> aria-current="true"<% } %>>`
       Do NOT remove `data-lang`, `flag-icon`, `flag-eng`/`flag-heb` (JS click hooks). Leave the inline flag SVGs untouched.
  </action>
  <acceptance_criteria>
    - header.ejs contains the literal `$ USD` and the literal `₪ ILS`
    - header.ejs no longer contains `דולר` or `שקל` (old USD/ILS Hebrew labels removed)
    - header.ejs still contains the default option bilingual `Currency` and `מטבע`
    - header.ejs still contains `value="usd"`, `value="ils"`, `value="default"`, `class="currency-option"`, `id="currency-desktop"`, `header-currency-selector`
    - header.ejs contains `title="English"` and `title="עברית"`
    - header.ejs contains `aria-current="true"` exactly twice (once per flag, each behind its language conditional)
    - header.ejs still contains `data-lang="eng"`, `data-lang="heb"`, `flag-eng`, `flag-heb`
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Development/Online && rg -n "\\\$ USD|₪ ILS|aria-current|title=\"English\"|title=\"עברית\"" backend/views/partials/header.ejs</automated>
    <automated>cd C:/Development/Online && rg -n "דולר|שקל" backend/views/partials/header.ejs; echo "expect: no matches"</automated>
  </verify>
  <done>USD/ILS options read `$ USD` / `₪ ILS` for both languages; flags have title + per-language aria-current; all JS hooks and option values intact.</done>
</task>

<task type="auto">
  <name>Task 2: View.js — mirror `$ USD`/`₪ ILS` labels + verify no destructive header rewrites</name>
  <read_first>
    - frontend/js/View.js (lines 978-1016 hydratePrototypeChrome; lines 1047-1071 updateCurrencySelectorText)
    - .planning/phases/39-header-utilities-layout/39-UI-SPEC.md (§File Change Scope — View.js)
    - CLAUDE.md (§SSR + Client Dual-Render)
  </read_first>
  <action>
    Part A — Mirror the SSR labels (required so Task 1's `$ USD`/`₪ ILS` are not reverted on hydration). In `frontend/js/View.js` `updateCurrencySelectorText` (lines 1053-1062), change the option-text assignments so BOTH language branches use the language-agnostic labels:
      - eng branch: `currencySelect.options[1].text = '$ USD';` and `currencySelect.options[2].text = '₪ ILS';` (keep `options[0].text = 'Currency';` and `currencySelect.removeAttribute('dir');`)
      - heb branch: `currencySelect.options[1].text = '$ USD';` and `currencySelect.options[2].text = '₪ ILS';` (keep `options[0].text = 'מטבע';` and `currencySelect.setAttribute('dir', 'rtl');`)
    Do NOT change the selected-value preservation logic (lines 1064-1069) or the `options.length >= 3` guard. Do NOT touch `getCurrencySelectorMarkup` (legacy .menu path).

    Part B — Verify-only audit (no edits expected). Confirm `hydratePrototypeChrome` (lines 981-1016) performs NO `innerHTML` assignment to the header / `.tk-nav` / `.tk-nav__utils` and does not rebuild flag/cart/currency markup — it only: binds flag-click navigation, calls `updateCurrencySelectorText`, and calls `persistCartNumber`. This means the CSS changes in Plan 01 (flag opacity, chevron, RTL block) are NOT undone by JS. If — and only if — you find an `innerHTML` rewrite of the prototype header inside `hydratePrototypeChrome`, STOP and report it (do not "fix" it by adding more JS). Record the audit result in the SUMMARY.
  </action>
  <acceptance_criteria>
    - View.js `updateCurrencySelectorText` assigns `options[1].text = '$ USD'` and `options[2].text = '₪ ILS'` in BOTH the eng and heb branches
    - View.js `updateCurrencySelectorText` no longer assigns the literals `'USD'`, `'ILS'`, `'דולר'`, or `'שקל'` to options[1]/options[2].text
    - eng branch still sets `options[0].text = 'Currency'` and `removeAttribute('dir')`; heb branch still sets `options[0].text = 'מטבע'` and `setAttribute('dir', 'rtl')`
    - `hydratePrototypeChrome` body (lines 981-1016) contains NO `.innerHTML` assignment (grep returns 0 within that function)
    - `getCurrencySelectorMarkup` is unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Development/Online && rg -n "options\[1\].text = '\\\$ USD'|options\[2\].text = '₪ ILS'" frontend/js/View.js</automated>
    <automated>cd C:/Development/Online && rg -n "innerHTML" frontend/js/View.js | rg -n "98[0-9]|99[0-9]|100[0-9]|101[0-6]" ; echo "expect: no innerHTML in hydratePrototypeChrome (981-1016)"</automated>
  </verify>
  <done>Currency labels persist as `$ USD`/`₪ ILS` after hydration in both languages; audit confirms hydratePrototypeChrome makes no destructive header innerHTML rewrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Boundary: browser DOM <- SSR template / hydration JS. Content rendered is static literal text (`$ USD`, `₪ ILS`, flag aria/title) — no user-supplied data is interpolated into the changed markup.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | T (Tampering)/I | header.ejs option labels + View.js option.text | accept | All inserted values are hard-coded string literals; no template interpolation of request/user data, so no XSS/injection surface introduced |
| T-39-03 | R (Repudiation)/a11y | flag aria-current/title | mitigate | Accessible name + active-state cue added per UI-SPEC so the icon-only language control is operable by AT |
</threat_model>

<verification>
- `header.ejs`: USD/ILS options read `$ USD` / `₪ ILS`; default stays bilingual; flags have title + aria-current; values/hooks intact.
- `View.js`: `updateCurrencySelectorText` emits `$ USD` / `₪ ILS` in both branches; hydratePrototypeChrome audit confirms no destructive header rewrite.
- Manual (deferred to phase verification): load `/en` and `/he`, open the currency dropdown, confirm `$ USD` and `₪ ILS` persist after the page settles (no flash-revert).
</verification>

<success_criteria>
All acceptance_criteria pass via grep; only `header.ejs` and `View.js` modified; no change to `getCurrencySelectorMarkup` or any header `innerHTML` path.
</success_criteria>

<output>
After completion, create `.planning/phases/39-header-utilities-layout/39-02-SUMMARY.md` (include the hydratePrototypeChrome audit result).
</output>
